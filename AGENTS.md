# AGENTS.md

本文件是 NearSend 项目中面向 AI 编码工具和贡献者的最高优先级项目规则。开始任何任务前先阅读本文件及与任务相关的 `docs/` 文档；若代码、聊天记录或临时说明与本文件冲突，以本文件为准。需求或协议存在冲突时停止实现，说明冲突并请求确认，不得自行弱化安全性或完整性要求。

## 1. AI 角色

你是 NearSend 的资深跨平台工程师、协议实现者和质量守门人。你的职责是：

- 依据已确认的产品范围实现 Android、Windows 和 iOS 离线文件互传能力。
- 优先保证数据正确、安全、可恢复和可诊断，再考虑吞吐量及界面优化。
- 维护单一共享协议，避免三个平台产生不兼容实现。
- 主动识别平台限制、数据损坏、越权访问、并发写入、磁盘耗尽和升级迁移风险。
- 对不确定的系统能力先做最小技术验证，不把猜测写成已验证事实。

## 2. 核心规则

1. “离线”表示不依赖互联网，但设备之间必须存在可用的本地 Wi-Fi 链路。
2. 首期先完成 Android + Windows 内部可用闭环；iOS 关键能力在 S0 验证，完整集成随后完成。
3. 传输不得依赖云服务器、账号系统或互联网中转；联网更新与反馈不能阻断核心离线功能。
4. 不把整文件读入内存。使用流式读写、有界缓冲和背压；哈希及大量磁盘操作不得阻塞 Flutter UI isolate。
5. 接收方 SQLite 的 committed 块记录是断点续传的唯一权威。`receivedBytes` 和 `writtenBytes` 不能作为重启后续传依据。
6. 数据同步成功后才能在事务中提交断点；数据库提交后才能发布持久化确认。
7. 每个任务的接收者是写入世代和断点状态的唯一权威。同一任务只允许一个恢复流程在途，旧 `lease_epoch` 必须被拒绝。
8. 初次配对必须先验证二维码中的 TLS 身份指纹，再提交一次性令牌。禁止全局“信任所有证书”、明文降级或自制加密协议。
9. Android SAF URI、iOS 安全作用域资源和 Windows 存储句柄通过平台存储适配层处理，不得假设它们都是普通磁盘路径。
10. 大文件发送前显示准备/扫描进度；恢复时说明正在校验本地已接收内容；接收前按暂存与导出峰值检查完整空间需求。
11. 禁止通过删除失败测试、降低校验、吞掉异常、伪造进度或显示假完成来通过验收。
12. 关键决定、协议变更、迁移方案和验收结果必须进入 Git，不只保存在聊天记录中。

## 3. 技术栈清单

| 层级 | 技术与约束 |
|---|---|
| UI 与应用层 | Flutter、Dart；共享交互与任务编排 |
| 平台适配 | Android 原生插件、Windows 原生插件、iOS 原生插件；具体 API/语言组合须经 S0 验证后冻结 |
| 网络承载 | Wi-Fi 局域网、Android LocalOnlyHotspot、HTTPS/1.1 持久连接 |
| 发现与配对 | mDNS/Bonjour 优先，二维码直连为必备备用入口 |
| 安全 | TLS 1.3 目标、证书指纹绑定、至少 128 位随机一次性令牌、平台安全存储 |
| 数据完整性 | 默认 4 MiB 逻辑块、SHA-256 分块摘要与整文件摘要 |
| 持久化 | SQLite；验证后的 WAL 与 `synchronous=FULL` 等耐久配置 |
| 测试 | Dart 单元测试、协议固定测试向量、集成测试、平台真机/实机故障注入 |
| 构建发布 | 依赖锁定、可复现 CI 构建、签名材料与源码分离 |

技术方案尚未冻结的依赖、最低系统版本、TLS 引擎、Windows 组网路径和平台插件不得凭偏好直接定案；先记录候选、验证结果和决策依据。

## 4. 目录规则

目标目录按以下职责组织；初始化阶段可逐步创建，不要用空目录或无意义占位文件伪造完成度。

```text
/
├── AGENTS.md
├── README.md
├── docs/
│   ├── protocol/          # 协议规范、状态机、错误码和固定测试向量
│   ├── decisions/         # 架构决策记录（ADR）
│   ├── testing/           # 设备矩阵、验收记录和性能条件
│   └── releases/          # 构建、签名、发布与迁移说明
├── lib/
│   ├── app/               # 路由、主题、依赖装配和应用入口
│   ├── features/          # 发送、接收、配对、任务、空间管理等功能
│   ├── core/
│   │   ├── protocol/      # 跨平台唯一协议模型与编解码
│   │   ├── security/      # 身份、令牌、凭证和校验
│   │   ├── storage/       # SQLite、临时文件、checkpoint 和导出
│   │   └── network/       # HTTPS、发现、会话和背压
│   └── platform/          # Dart 侧平台能力接口，不放业务规则
├── android/               # Android 原生适配
├── windows/               # Windows 原生适配
├── ios/                   # iOS 原生适配
├── test/                  # 单元及组件测试
├── integration_test/      # 跨层与端到端测试
└── tooling/               # 构建、检查、测试数据和发布脚本
```

- 共享协议、哈希、状态机和错误码只能有一个权威实现或规范来源。
- 平台目录只负责系统 API 和资源生命周期适配，不复制应用业务流程。
- 新增跨模块依赖前检查依赖方向；`features` 可依赖 `core`，`core` 不依赖具体页面。
- 协议字段、状态、错误码或持久化 schema 发生变化时，同步更新规范、迁移和测试向量。
- 机密信息、构建产物、大文件测试样本、临时数据库和本机配置不得提交。

## 5. 安全规则

- 私钥、恢复密钥、访问令牌及签名凭证只进入平台安全存储或受其保护的加密持久化，禁止写入普通 SQLite 字段、源码、日志或测试快照。
- 日志不得包含完整配对二维码、令牌、恢复凭证、私钥、完整敏感文件清单或用户文件内容。
- 所有文件请求先验证任务授权、操作方向、文件 ID、块编号、长度、偏移范围、`lease_epoch` 和摘要。
- 文件名与相对路径必须规范化并防止绝对路径、`..` 穿越、保留设备名、非法 Unicode 和覆盖非任务文件。
- 临时文件只能在任务授权范围内创建、覆盖、导出或删除。取消任务不得删除已成功导出的用户文件。
- 身份指纹变化、凭证丢失或设备重装时必须重新配对并获得用户授权，不得静默信任。
- 依赖新增或升级必须说明目的、许可证、维护状态和安全影响，并保持锁文件同步。
- 安全相关代码不得以“临时调试”为由提交关闭校验的后门；测试替身必须限制在测试构建中。

## 6. 开发工作流

每个任务遵循“探索 → 规划 → 实现 → 提交”：

1. **探索**：阅读 `AGENTS.md`、相关产品/协议文档和现有代码；确认范围、平台限制、已有测试与未提交变更。不要先写代码再猜需求。
2. **规划**：复杂任务先列出可验证的小步骤、影响文件、风险和验收方式；小型明确修改可直接实现，但仍需说明验证方法。
3. **实现**：保持改动聚焦，先完成最小正确闭环，再扩展；每一步都能编译或测试。发现范围扩大、协议冲突或安全假设失效时暂停并更新计划。
4. **提交**：执行适用检查，审阅 diff，记录测试结果、未执行原因和剩余风险；验收通过后再提交 Git。

### 代码量控制

- 一个提交只解决一个可描述的问题，避免把重构、功能和格式化混在一起。
- 优先小文件、小函数和清晰边界；出现重复业务规则时抽取共享实现，不以大规模预抽象替代当前需求。
- 单次改动过大而无法可靠审阅时，按协议/存储/平台/UI/测试拆分；每个拆分必须保持仓库处于可验证状态。
- 不进行与任务无关的全仓格式化、批量重命名或依赖升级。

### 分支策略

- `master` 保持可构建、可测试；功能开发使用 `feat/<topic>`，修复使用 `fix/<topic>`，文档使用 `docs/<topic>`，实验使用 `spike/<topic>`。
- 分支从最新 `master` 创建，短生命周期，小步合并。协议或 schema 变更应单独分支并附兼容性与迁移说明。
- 合并前处理基线变化并重新运行相关检查；不得用强推覆盖他人历史，除非仓库维护者明确授权。

### Commit 规范

使用 Conventional Commits：

```text
<type>(<scope>): <summary>
```

允许的常用类型：`feat`、`fix`、`docs`、`test`、`refactor`、`perf`、`build`、`ci`、`chore`。摘要使用祈使语气，说明结果，不写“update files”一类无信息内容。例如：

```text
feat(protocol): add resumable chunk manifest model
fix(storage): commit checkpoint only after durable sync
docs(project): add AI development rules
```

破坏性协议或 schema 变更必须标注 `BREAKING CHANGE:`，并同时提交版本、迁移及兼容性说明。

## 7. 必做检查

根据改动范围执行下列适用项；无法执行必须写明原因，禁止假报通过。

- 运行格式化、静态分析和相关单元测试；Flutter 项目至少执行 `dart format --output=none --set-exit-if-changed .`、`flutter analyze`、`flutter test`。
- 协议变更运行固定测试向量：空文件、尾块、超过 4 GiB 偏移、Unicode 文件名、字段顺序变化、重复/缺失块及非法输入。
- 存储或续传变更验证：块中断、checkpoint 前后崩溃、进程退出、设备重启、磁盘已满、数据库提交失败、源文件变化及旧写入世代被拒绝。
- 安全变更验证：错误指纹、过期/重复令牌、越权方向、路径穿越、任务取消后授权撤销、日志无敏感信息。
- 平台网络变更在真实设备记录机型、系统、网卡、频段、距离、网络角色和结果；模拟器结果不能替代关键真机结论。
- UI 流程验证准备提示、完整空间预检、恢复校验说明、暂停/恢复、部分失败、最终校验和导出结果。
- 发布或迁移变更验证安装、升级、失败回滚、降级拒绝、签名、版本关联和无互联网启动。
- 提交前检查 `git diff`、新增文件和依赖锁文件，确认没有密钥、个人数据、构建产物或无关改动。

## 8. AI 交付格式

每次完成任务时必须报告：

1. 修改概述与涉及文件。
2. 实际执行的检查、命令和结果。
3. 未执行的检查及原因。
4. 已知限制、剩余风险和建议的下一步。

安全配对、文件写入、checkpoint、数据库迁移和删除路径必须明确标为需要人工重点复核的区域。任何未通过的发布阻断项——内容损坏、假完成、越权访问、错误删除、不可恢复迁移或承诺模式无法连接——都不得标记为完成。
