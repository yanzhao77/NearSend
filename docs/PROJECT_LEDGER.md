# NearSend 项目现状与进度台账

更新日期：2026-09-20。范围：技术方案V2.1、协议草案、S0参考实验、研发治理与实施规格。后续以本文件最新Git版本为准，不用聊天记录代替状态。

## 一句话现状

**S0进行中：产品方向、实施架构、端侧分层、UI/UX、质量门禁和 Agent 工作流已形成设计基线；协议草案与 Python 参考实验已完成；T01-01 已建立 Android/Windows/iOS 三端 Flutter 工程与版本可追踪的壳应用，并在 Android 真机与 Windows 上构建运行通过；T01-02 已把必做检查与双端构建固化为 GitHub Actions 门禁并在真实运行中全部通过。传输、配对、发现、存储与恢复功能均未实现；没有可分发安装包或正式发布版本；协议与关键平台选型尚未冻结。**

## 1. 状态口径

- 已完成：限定范围内已有文件及可核对证据，不自动等于产品功能完成。
- 部分完成：某一参考层通过，仍有明确退出门槛。
- 阻塞：缺必要环境/设备，未执行，不计为通过或失败。
- 待开始：尚无该产品模块实现。
- 待复核：需要人工或独立实现验证，不用同一参考代码的自测替代。

不计算没有依据的整体完成百分比，不将Linux实验或HTTP loopback称为跨平台验收。

## 2. 成果台账

| ID | 成果 | 状态 | 证据/位置 | 限制及下一步 |
| --- | --- | --- | --- | --- |
| D01 | 定位与技术方案V2.1 | 已完成：设计 | [方案](跨平台离线文件互传系统技术方案_V2.1.md) | 保留现有产品摘要和发布策略；不是实现证明 |
| D02 | AI开发规则 | 已完成：规则 | [AGENTS.md](../AGENTS.md) | 本次未改动 |
| D03 | 协议v1.0-draft1 | 已完成：草案；未冻结 | [协议](protocol/v1.0-draft1.md) | 待独立实现、接口鉴权及平台验证 |
| D04 | 固定编码/摘要向量 | 部分完成：已有 Dart 独立比对 | [向量](protocol/vectors-v1.json)、[Dart 实现](../lib/core/protocol/manifest.dart)、[T02-01 证据](testing/evidence/2026-09-20/t02-01-01/summary.md) | 三个向量的 LFTM1/LFTC1 字节与摘要已被 Dart 独立复现一致；仍待 Kotlin/Swift/Windows 独立实现比对 |
| E01 | 24项自动化实验 | 已完成：Linux参考层 | [原日志](testing/evidence/2026-09-20/unit-tests.log)、[导入复测](testing/evidence/2026-09-20/repository-import-tests.log) | 不覆盖完整产品API和设备生命周期 |
| E02 | TLS1.3与pin正反例 | 已完成：本机loopback | 同E01、[探针](../tooling/s0/test_probes.py) | 待三端引擎及网络绑定验证 |
| E03 | 崩溃窗口与损坏块修复 | 已完成：本地参考层 | 同E01、[存储探针](../tooling/s0/storage_probe.py) | 子进程退出；非断电、非平台生产存储 |
| E04 | 1GiB及20GiB真实落盘回读 | 已完成：Linux合成数据 | [1GiB](testing/evidence/2026-09-20/large-1gib.json)、[20GiB](testing/evidence/2026-09-20/large-20gib.json) | 无网络/任务DB/导出；本次导入未重跑 |
| E05 | 1,000/10,000小文件及100空文件 | 已完成：本地存储层 | [结果](testing/evidence/2026-09-20/small-files.json) | 不包含UI、网络、混合队列；本次导入未重跑 |
| D05 | 真机执行单与限制 | 已完成：执行方案 | [S0报告](testing/S0-report.md) | 执行单不是执行结果 |
| D06 | 文档中心与权威关系 | 已完成：设计 | [文档中心](README.md) | 作为文档入口，不代表功能实现 |
| D07 | Vibe Coding 开发流程 | 已完成：治理 | [流程](DEVELOPMENT_WORKFLOW.md) | 后续任务须形成需求—PR—证据—台账闭环 |
| D08 | 系统架构 | 已完成：实施草案 | [架构](architecture/SYSTEM_ARCHITECTURE.md) | 平台依赖与部分接口待 S0 冻结 |
| D09 | 应用与端侧服务设计 | 已完成：实施草案 | [端侧设计](architecture/APP_AND_SERVICE_DESIGN.md) | NearSend 无中心云后台；具体库待验证 |
| D10 | UI/UX 与视觉基线 | 已完成：视觉设计 | [设计入口](ui/README.md)、[样式指南](ui/STYLE_GUIDE.md) | 已提供组件、移动端和 Windows SVG 视觉稿；待 Flutter 实现与可访问性验证 |
| D11 | 质量与验收策略 | 已完成：设计 | [质量策略](testing/QUALITY_AND_ACCEPTANCE.md) | 设备阈值待目标端基线形成后冻结 |
| D12 | Agent 任务手册 | 已完成：治理 | [任务手册](AGENT_TASK_PLAYBOOK.md) | 任务卡统一放在 [docs/tasks/](tasks/README.md)，见 ADR-0001 |
| R01 | Flutter客户端与原生适配 | 部分完成：工程基线 | [T01-01](tasks/T01-01.md)、[T01-01 证据](testing/evidence/2026-09-20/t01-01-01/summary.md) | Android/Windows/iOS 工程可构建；Android 真机与 Windows 均启动同一版本壳应用；传输、配对、发现、存储、恢复未实现 |
| R02 | 安装包、签名构建及发布CI | 待开始 | 无产物 | 当前只有发布策略，未发布任何版本 |
| R03 | CI 门禁与工具链固定 | 已完成 | [T01-02](tasks/T01-02.md)、[运行汇总](testing/evidence/2026-09-20/t01-02-01/summary.md) | 四个作业在 GitHub Actions 真实通过；签名与发布作业仍属 T10 |
| R04 | 接收方持久化层 | 部分完成：实现并测试 | [T04-01](tasks/T04-01.md)、[运行汇总](testing/evidence/2026-09-20/t04-01-04/summary.md) | schema v1、迁移与回滚、高版本拒绝、chunk 提交顺序与写入世代、幂等记录、任务/文件状态、导出记录、peer 授权、引擎真实 `SQLITE_FULL` 注入均已实现；**未完成**：真实暂存损坏、OS 层满盘（`ENOSPC`）、多版本迁移、真机 `syncData` 耐久性（B04）。全部结论来自 Windows 桌面测试，不得当作真机结论 |

历史实验环境见[environment.json](testing/evidence/2026-09-20/environment.json)。20GiB整文件哈希通过；峰值RSS为18,944KiB，1GiB为18,816KiB。该口径不包含系统页缓存，不代表真机吞吐量或完整应用内存。

## 3. S0门槛与阻塞台账

| ID | 优先级 | 项目 | 当前状态/阻塞 | 下一动作 | 完成标准 |
| --- | --- | --- | --- | --- | --- |
| B01 | P0 | Android热点与Windows加入 | 环境已具备（T01-01 安装 Flutter/Android SDK 并接入真机 25102RKBEC / Android 17）；探针未执行。限制：Windows 侧只有本机 Win10 + Intel AC-7265，且无第二台 Android 设备 | 准备原生探针，关闭上游网络测试 | 实际双向样本传输、重建热点重新认证 |
| B02 | P0 | Android指定Network与TLS衔接 | 真机与 SDK 已具备；纯Dart路径仍未确认 | 对比原生与Dart适配路径 | 多网络下走正确接口，pin前不发凭证 |
| B03 | P0 | URI重开、seek、权限与暂存 | 真机已具备；尚无真实文档提供者样本 | 本地/外接/云占位文件分别测试 | 重启重开或明确暂存/阻断路径 |
| B04 | P0 | 真实存储checkpoint及恢复 | 仍仅Linux参考层通过；真机存储后端未执行 | 真机存储后端执行故障窗口 | 不假提交，恢复最终哈希一致 |
| B05 | P0 | 协议跨语言与完整API | 只有Python子集 | 实现独立编码器及认证/分页/恢复接口 | 向量一致，授权与幂等正反例通过 |
| B06 | P1 | iOS关键能力探针 | 缺macOS/Xcode/iPhone | 早期验证TLS、局域网权限、文件生命周期 | 前后台中断后可靠恢复，记录实际边界 |
| B07 | P1 | 真机大文件与队列 | 尚无完整产品链路 | 先小样本，再20GiB及小文件集合 | 准备/传输/终检/导出全流程证据 |
| B08 | P2 | Windows自动离线组网 | 缺Windows环境/网卡矩阵 | 独立实验条件能力 | 按设备列支持范围，不阻塞Android热点主路径 |

责任分工：目标设备与开发环境由维护者提供或指定；探针实现、测试执行与证据更新由后续开发任务承担。当前未给任何具体人员分派GitHub任务，也没有未确认的截止日期。

## 4. 开发任务台账

| 任务 | 当前状态 | 退出门槛 |
| --- | --- | --- |
| T00 文档与研发治理 | 已完成：设计 | 文档索引、流程、架构、端侧、UI、质量和 Agent 手册已提交；后续持续维护 |
| T01 工程/构建/配置 | 已完成 | 两端可安装/运行、版本可追踪（§6.1）；检查与双端构建已固化为 CI 门禁（§6.2） |
| T02 协议/模型/向量 | 部分完成：模型已收敛，但**发现三处草案缺口** | 草案、Python 向量与 Dart 独立实现逐字节一致（T02-01）；错误码/状态机/协议协商/幂等已实现（T02-02）；**冻结被 §5 登记的三项草案缺口阻塞**，另需原生实现比对 |
| T03 配对与单文件链路 | 部分完成：配对载荷、信任上下文、令牌生命周期、`/v1/pair` 模型、`/v1` 线上契约层、§7 路由表、§6 清单分页/seal、§7 响应体、**§8 块头规则与批量 checkpoint 窗口（含 schema v3）**已实现；端点逻辑与网络绑定未做 | 用户授权后端到端收发、鉴权负例通过 |
| T04 分块与checkpoint | 部分完成：存储层已实现并测试（schema/迁移/块权威/租约/幂等/任务与文件状态/导出记录/peer 授权），实验 | 生产存储实现、批量窗口及背压验证 |
| T05 暂停/故障/重启恢复 | 部分完成：实验 | 双端真实进程/设备生命周期恢复 |
| T06 终检/导出/空间检查 | 部分完成：空间计划、整文件终检、导出门禁、安全命名、导出编排与清理（含 schema v2）已实现；平台 `ExportSink` 实现、取消路径与调度接入待做 | 保存失败可重试且不重复导出 |
| T07 Android热点集成 | 阻塞于B01/B02 | 无互联网、无路由器端到端完成 |
| T08 反向/多文件 | 待开始 | 角色矩阵、部分失败、小文件队列通过 |
| T09 iOS集成 | 待开始，早期探针阻塞 | 六方向与iOS生命周期通过 |
| T10 安装升级与发布 | 待开始 | 迁移、兼容、签名与用户独立试用通过 |

### 4.1 首批可领取任务

| 子任务 | 依赖 | 当前状态 | 退出门槛 |
| --- | --- | --- | --- |
| T01-01 Flutter 工程基线 | 无 | 已完成 | Android/Windows 启动同一壳应用，版本可追踪 |
| T01-02 CI 与检查 | T01-01 | 已完成 | format/analyze/test/build 可重复运行 |
| T02-01 Dart canonical manifest | T01-01、D03/D04 | 已完成 | 固定向量正反例一致，不复制 Python 实现逻辑 |
| T02-02 状态/错误/版本模型 | T01-01、D03 | 已完成 | 模型和序列化测试通过，未知字段规则明确 |
| T04-01 SQLite schema 与 chunk repository | B04 设备验证可后补 | 进行中（schema+迁移含 **v1→v2 端到端**、chunk repository、幂等、状态与导出记录、peer 授权、磁盘满注入已实现并测试；真实暂存损坏、OS 层满盘、真机 syncData 未完成） | durable 提交顺序、迁移和故障测试通过 |
| T03-01 同网二维码配对 | T01-01/T02-02/**B02（仅网络绑定部分）** | 进行中（配对载荷、信任上下文、令牌生命周期与限流、`/v1/pair` 模型、`/v1` 线上契约层、**§7 的 19 行路由表与参数校验**、**§6 清单分页与 seal**、**§7 响应体**、**§8 块请求/响应头与块体长度规则**、**§8 批量 checkpoint 窗口与 `checkpointSeq`（schema v3）** 已实现并测试；**目标端网络绑定仍阻塞于 B02**；§9 状态体、§8 完整写入顺序（文件锁、等待旧写入停止）、每文件串行化与背压、19 个端点的实际逻辑、HTTP 服务器与 TLS 引擎未做） | pin 先于令牌，正负例和目标端网络绑定通过 |
| T06-01 空间计划、终检与导出 | T01-01/T04-01 | 进行中（空间计划、整文件终检、导出门禁与安全命名、导出编排与清理、schema v2 已实现并测试；平台 `ExportSink`、取消路径与调度接入未完成） | 分卷明细、unknown/不足、终检和幂等导出通过 |

领取和交付格式见 [Agent 任务手册](AGENT_TASK_PLAYBOOK.md)。子任务未满足依赖时不得通过演示代码绕过安全或持久化要求。

README的S1表示协议冻结阶段，对应T02后半段；S2/S3/S4是展示路线图。正式开发依赖与P0/P1/P2里程碑以技术方案第十三章为准，不能把S1“草案完成”解释为冻结完成。

## 5. 待人工重点复核

| 范围 | 复核要求 | 状态 |
| --- | --- | --- |
| TLS配对与凭证 | pin来源、所有证书必比对、令牌交付/撤销 | 待复核；本次仅验证loopback pin。T04-01 已实现 peer 身份指纹与用户授权的持久化：指纹变化只**报告**不吸收，只有显式用户授权才改变已存信任，且 `peers` 表结构被测试固定为无令牌/密钥列。**配对流程与 UI 尚未接上该入口** |
| 文件写入与checkpoint | 同步顺序、写入栅栏、实际平台耐久性 | 待真机及人工复核 |
| 协议冻结 | 双向角色、分页快照、恢复幂等与版本规则 | LFTM1/LFTC1 已由 Dart 独立实现复现一致（T02-01）；错误码/状态机/协商/幂等已收敛为可测试模型（T02-02）；**仍未冻结**，被下列三项草案缺口与原生实现比对阻塞 |
| **草案缺口：能力词表** | §3 写作 `capabilities:[...]` 却未定义任何能力标识；§17.1 要求「共有能力覆盖任务需求」 | **未定义，需维护者决定**。T02-02 已实现协商算法并用合成标识验证，但**不发明词表**（`AGENTS.md` §3）。**冻结前必须确定词表与「任务所需能力」的定义** |
| **草案缺口：终态无出边** | §10 只说明如何进入 `BLOCKED`/`FAILED`/`PARTIALLY_COMPLETED`，未定义如何离开 | **`failed` 与 `partiallyCompleted` 完全没有出边**，与 V2.1 §16.1「只重试失败项」冲突；**`blocked` 唯一出边是取消**，与 §11 对 `SPACE_INSUFFICIENT`「清理/换位置后重试」冲突，且取消会丢弃被承诺保留的恢复数据。已由 `statesWithoutDefinedExit` 与 `statesThatCanOnlyBeCancelled` 断言固定。**属协议变更，冻结前必须解决** |
| **草案缺口：文件级重试** | 文件状态机的 `failed` 无出边，无法表达「只重试失败项」 | 以 `FileStateMachine.retryOfFailedFilesIsDefined = false` 显式暴露并断言。**冻结前必须解决** |
| **NFC 路径强制** | 协议 §5.1 要求接收端拒绝非 NFC 规范的相对路径；检测需要 Unicode 规范化实现，Dart SDK 不提供 | **未实现，已登记为待人工决策**：选择实现属于依赖决策（`AGENTS.md` §3 禁止凭偏好定案）。候选：① 引入纯 Dart 规范化包；② 调用平台原生规范化 API；③ 由发送端负责并对摘要附加规范化声明。缺口以 `RelativePathRules.enforcesNfcNormalisation` 与一条断言显式暴露。**必须在协议冻结前解决** |
| 加密原语依赖 | SHA-256 实现的选择必须记录理由、许可证与安全影响 | 已引入 `crypto` 3.0.7（Dart 团队、BSD-3、纯 Dart、不接触文件/网络/密钥），见 [ADR-0002](decisions/ADR-0002-crypto依赖与SHA256.md)；自制加密算法已被明确否决 |
| SQLite 绑定 | `SYSTEM_ARCHITECTURE.md` §12 把 SQLite 插件列为待冻结；选择必须记录候选与验证结果 | 已选用 `sqlite3` 3.6.0（MIT、跨三端、可显式控制 PRAGMA 与事务），见 [ADR-0003](decisions/ADR-0003-SQLite绑定与耐久性配置.md)；探针在本机 5/5 通过（SQLite 3.53.4、`synchronous=FULL` 读回为 2）。**已移除已废弃且为空的 `sqlite3_flutter_libs`（`0.6.0+eol`）** |
| 存储引擎错误分类 | 只有被测试真实产生过的引擎错误才可映射为特定语义，未映射的 `SQLITE_*` 会一律落到「可重试的提交失败」 | **部分复核**：`SQLITE_FULL`（主码 13）已映射为 `NS-STORAGE-008 spaceInsufficient` → `SPACE_INSUFFICIENT`（507，**不可重试**），由引擎真实产生并由测试固定。`SQLITE_READONLY` / `SQLITE_IOERR` / `SQLITE_CORRUPT` 等**尚未映射**，落点语义未必正确，**需人工判断是否逐项处理**；本次不凭推测扩充映射 |
| 平台 durable sync 语义 | 协议要求「durable sync 后才提交块」；`syncData` 的真实平台语义须由真机验证 | **未验证**。ADR-0003 只证明 PRAGMA 被接受与事务原子性，**不得**据此声称断电耐久性已验证；该结论属 B04 真机故障注入 |
| 导出/删除/迁移 | 不误删、不重复导出、迁移失败保留数据 | **部分复核**：导出**记录**层已实现并测试（仅在文件全部 committed 后记录；同一文件不静默产生第二份副本；失败的导出不覆盖已保存记录）；迁移失败保留数据与高版本拒绝先于任何 PRAGMA 已由测试覆盖。**导出文件本身的写入、删除与清理尚未实现（T06-01）**，不得据此声称「不误删」已复核 |
| 空间卷标识 | 同一物理卷必须被当作同一个卷核算，否则同一份空间会被检查两次而**低估**需求 | **设计上唯一会导致低估的入口**：`SpacePlan` 按 `VolumeId` 值相等分组，取值责任在调用方。平台若无法给出稳定 provider 标识，调用方必须传保守（宁可过粗）的标识。**`FileSink` 实现时必须重点复核**，见 [空间计划证据](testing/evidence/2026-09-20/t06-01-01/summary.md) §7 |
| 空间余量策略 | 安全余量是可调整策略，**不是**空间足够的保证（V2.1 §16.2） | 已实现为 `SafetyMarginPolicy`（默认 `max(256 MiB, 1%)`，逐卷），并由测试断言余量文案与数值均表达「策略而非保证」。调整该策略必须先说明新余量为何仍然够用 |
| 终检与导出的强制关系 | 「完成 = 终检通过且导出结果已提交」必须由**代码**保证，不能只靠调用约定 | **已修（`t06-01-03`）**：`recordSavedExport` 把 `verification` 改为**必填**，并校验回执属于该文件、可导出、且覆盖的字节数等于文件**当前**大小。新增门禁用例覆盖「所有块 committed 但整文件摘要不一致」——旧块数检查看不见的情况。**残余**：Dart 无 friend class，回执构造函数公开，故不防御**应用内伪造**（关闭的是遗漏路径，不是恶意代码） |
| 回执可伪造性 | 只有 `FileVerifier` 应能签发「已验证」回执 | **未解决，需人工决策**：可选 ① 把 `FileVerificationResult` 构造函数收为库私有并由 `FileVerifier` 唯一签发（测试需经真实终检，成本较高）；② 引入仅库内可构造的回执类型。当前选择是必填参数 + 字段校验，已关闭遗漏路径 |
| 目标已有条目的枚举完整性 | 导出不得覆盖用户已有文件；枚举不全时策略会认为名字空闲 | **本模块唯一会导致数据丢失的入口**：`takenPaths` 由调用方提供。`t06-01-04` 已让编排在**清点失败时直接拒绝导出**（`ExportOutcomeKind.refused`）而不是当作「没有冲突」，并由测试固定；但平台 `ExportSink` 实现本身仍待做，且**清点与提交之间的竞态未处理** |
| 「无法证明导出提交结果」的窗口 | V2.1 §15：重启后检查已有目标或请用户确认，**不盲目新建重复文件** | **部分关闭（`t06-01-04`）**：提交与记录之间副本已在目标但本地不知道。写入前若发现自然名字下的条目**摘要相同**即复用而不重写。长度相同但摘要不同**不视为同一个**（同长度不是身份证明）。**残余**：提供者读不到摘要时无法证明，仍可能多一个 `(1)` 副本；缓解手段是导出记录检查与 `NameConflictPolicy.ask` |
| 「不误删用户文件」的保证方式 | `AGENTS.md` §5 与 V2.1 §16.1：取消不得删除已导出文件；删除已保存文件必须是独立明确操作 | **结构性保证（`t06-01-04`）**：`ExportSink` 端口**没有**删除/移动/覆盖目标条目的方法，因此没有任何代码路径能做到，测试以操作序列钉住。**复核时须把该缺失当作设计要求而非遗漏**——一旦有人给端口加上删除目标的方法，保证立刻消失。取消流程本身仍未实现 |
| 旧的 `saved_path` 为 `NULL` 的导出记录 | v1 未记录落盘名字；回退到冻结路径可能报错位置 | **需人工决定**：`t06-01-04` 的「已保存到本目标」分支会回退到冻结路径来报告位置。若那次导出其实已改名，该回退就是错的。选项：请用户确认位置，或接受这个近似 |
| 坏块降级与写入世代 | 协议 §8 要求写入世代仲裁；`markChunksMissing` 目前不接受 `lease_epoch` | **需人工确认**：终检会调用降级，而该调用不受世代约束。缓解理由是降级只会**减少**信任（两个并发恢复降级同一批块不会产生假完成），但这一推理需人工确认，或改为给降级加世代前置条件 |
| 「pin 先于令牌」的保证方式 | `AGENTS.md` §2 规则 8 与协议 §2：必须在发送令牌、HTTP 请求及文件内容之前完成指纹比对 | **结构性保证（`t03-01-01`）**：令牌不是一个字段，只能经 `PairingHandshake.pairingToken` 取得，该 getter 在比对成功前抛 `PAIR_REJECTED`；比对失败关闭连接且不得重新协商；每连接隔离。**残余**：取得叶证书 DER 的 TLS 引擎未实现，**没有调度代码在调用**，因此未经端到端验证 |
| 指纹的三种写法 | 协议 §2 定义 pin 为 `SHA-256(叶证书完整 DER)`，**不是** PEM 文本摘要、**不是** SPKI 摘要 | `t03-01-01` 把参数类型定为字节（无 PEM 重载）、并有测试断言它与 PEM 文本摘要、与对 DER 一部分的摘要都不同。**弄错会得到一个看似合理却永远匹配不上的 pin**，因此这一项在实现 TLS 引擎时必须再次复核 |
| 配对候选地址的信任语义 | 协议 §3：候选地址选择**不得绕过 pin** | **需人工复核**：`t03-01-01` 只保证「拿到令牌必须先比对」，**选哪个候选**由调用方决定。网络层实现时必须确保无论选哪个候选，pin 都来自二维码本身而非连接结果。当前无网络层，故无端到端验证 |
| `expiresInSeconds` 的权威性 | 协议 §3：二维码里的值是**提示**，以服务端状态为准 | `t03-01-01` 只校验它为正数，**不**把它当过期时间。`t03-01-02` 的 issuer 用**自己的单调 300 秒窗口**判定，与二维码里的提示无关，测试固定了「到点即失效」。响应里的 1800 秒是会话令牌有效期，同样不得被当作「过期后仍可用」的依据 |
| 令牌比较与「常数时间」 | 比较秘密通常应常数时间，但实现必须诚实说明平台提供什么 | **有意不做常数时间（`t03-01-02`）**：Dart 的 JIT 可重排、运行时对普通比较无时序契约，手写循环会构成**虚假保证**。真正约束风险的是 256 位熵、单次消费、300 秒窗口、滑动限流与**只比较摘要**。若要改，必须先说明威胁模型变化 |
| 「只保存令牌摘要」未经测试 | `t03-01-02` 的 issuer 只保存 `SHA-256(令牌)`，不保存原值 | **代码层面性质，无测试覆盖**：Flutter 无 `dart:mirrors`，测试无法断言内部字段。复核时应以**代码审查**确认，不得依赖测试绿灯 |
| §9 幂等与 §3「不恢复已消费令牌」的张力 | §9 要求 `requestId` 幂等重放；§3 要求配对响应丢失后**不恢复**已消费令牌 | **需人工在冻结前定案**：配对是 §9 幂等的一个**例外**。`t03-01-02` 只校验 `requestId` 的规范 UUID 格式，**未实现**任何幂等重放语义，因为对配对而言重放正是 §3 禁止的事。需明确：是把配对排除在 §9 范围外，还是为配对定义「同一 requestId 返回同一拒绝」 |
| 限流与熵的关系 | 协议 §3：**限流不替代随机熵** | `t03-01-02` 的限流只是纵深防御；`generatePairingTokenBytes` 必须保持 32 字节平台 CSPRNG 熵，**不得**因存在限流而降低。限流状态目前仅在进程内，重启清零（§3 未要求持久化，需真机确认该读法）。`t03-01-03` 新增 `retryAfterSeconds`（供 `Retry-After`），从最早失败移出窗口推导 |
| 控制体解析器差分 | 协议 §4：拒绝重复对象键、深度 ≤16 | **已实现（`t03-01-03`）**：Dart 的 `jsonDecode` **不拒绝重复键**（保留最后一个、不报错），这正是解析器差分的形状，故先扫描结构再取值；扫描器**先解码转义再比较键**（`{"a":1,"\u0061":2}` 必须被拒）。**需人工确认**：扫描器目前只判结构、不取值——若将来给它加上取值逻辑，这个「分歧只可能更严」的论证就不再成立 |
| 错误体的 `message` | 协议 §7：message 不含密钥或完整本地路径 | **结构性保证（`t03-01-03`）**：`WireError.of(code)` **不接受** message 参数，只写入该码的 `messageKey`（封闭常量集），并有测试遍历全部错误码断言消息匹配 `^[A-Za-z0-9.]+$`。**收到的** message 仍是任意文本：只限长与查控制字符，**不**猜测是否含密钥——UI 渲染它时必须按不可信输入处理（目前无 UI 消费） |
| 错误体 `retryable` 的一致性 | §11 按错误码定死可重试行为 | **解析时校验（`t03-01-03`）**：`retryable` 必须与错误码在 §11 的行为一致，两个方向都拒绝。否则一个发来 `retryable:true` + `CHUNK_HASH_MISMATCH` 的对端能让客户端**永远重试一个损坏的块** |
| `Retry-After` 的上限 | §11 只规定「按 Retry-After（秒）退避」，**未设上限** | **需人工决定**：`t03-01-03` 只做格式校验（拒绝 HTTP-date、要求 429 带正数秒），一个对端可以要求等 999999999 秒。是否加客户端侧上限属产品/协议决定，本层不擅自设定 |
| §8 的块头规则 | 单一 `Content-Length`、不支持 `Transfer-Encoding`、不许压缩、同时出现必须拒绝、末尾多余数据拒绝并关闭连接 | **头规则已实现（`t03-01-07`）**，做成**对头映射求值的纯函数**：帧定界判定与「读 body」彻底分离，不合法的消息**一个字节都不会被处理**，且不需要 socket 即可测试。**「单一 `Content-Length`」在头映射里有两个形态**（只差大小写的两个条目／逗号连接的一个值），**两种都拒绝**——悄悄合并就是替实现选一个长度，而选哪一个正是这条规则要防的分歧。`X-LFT-` 前缀内的未定义头拒绝，其余无关头（`User-Agent` 等）放行。**残余**：「拒绝并**关闭连接**」的关闭那一半需要 HTTP 层；§8 的**写入顺序**里「认证和授权→文件锁」与「先撤销旧会话、**等待旧写入停止**」未接线；**每文件串行写入**与**背压**没有结构性保证（批量 checkpoint 窗口本身已在 `t03-01-08` 实现，见下） |
| 块体的预期长度由谁定 | §8：body 长度必须等于该块预期长度，末尾额外数据拒绝 | **已实现（`t03-01-07`）**：`assertChunkBodyLength` 只接受恰好等于预期值；短的决定为「消息提前结束」（**可能正是因为两个跳数对帧定界有分歧**），长的则是「在声明的块之后又追加了字节」。两者都答 `INVALID_FIELD`：§11 给该码的指示是「修正请求，**不自动原样重试**」，正是帧定界错误应有的指示——原样重发只会重演同一次分歧。**需确认**该码选择（§8 未指定错误码，本层选 400 而非 `CHUNK_HASH_MISMATCH` 422，理由是帧定界错误要**改请求**而不是**从正确来源修复**） |
| 块响应头的摘要是参考性的 | §8：`GET` 返回 `Content-Length` 与 `X-LFT-Chunk-SHA256`，**接收者仍以冻结清单为权威** | **已实现（`t03-01-07`）**：`agreesWithManifest` 如实报告对端的说法是否与清单一致，但**不把不一致变成拒绝**——若在参考性字段上拒绝，等于把一个对端本来影响不了的下载失败权交给它；同时保留头里的原值。与 `t03-01-05` 的「seal 摘要放最后」同源：摘要对每种问题都失败却不说哪一种 |
| `X-LFT-Lease-Epoch` 的规范拼写 | §8 只给出头名，未说取值文法 | **本读法（`t03-01-07`）**：用 §4 的规范十进制校验，即**拒绝前导零**（`0003` 非法），理由是让一个写入世代只有一种拼写。`Content-Length` 则**接受**前导零——它是 **HTTP 字段**，按 HTTP 文法是数字串；§4 的规范十进制是**协议字段**的规则，两者层次不同。**需确认**，若与对端不一致会对齐浪费一次往返 |
| `Content-Type` 是否允许参数 | §8 措辞为「要求 `Content-Type: application/octet-stream`」 | **按「恰好」理解（`t03-01-07`）**：`application/octet-stream; charset=binary` 被拒绝。两端都由本项目实现故可接受，但这是**可能影响互操作**的严格读法，**需确认** |
| `Transfer-Encoding: identity` 也被拒绝 | §8：「不支持 `Transfer-Encoding`」 | **保守读法（`t03-01-07`）**：出现即拒绝，**没有为 `identity` 开例外**——`identity` 是「未压缩」的声明，而 §8 拒绝的是这个头的存在本身。压缩同理：`Content-Encoding` 只接受缺省或 `identity`。**需确认** |
| 存储错误的线路码兜底分支 | 协议 §11 按错误码定死 HTTP 状态与可重试性；`AGENTS.md` §3 禁止发明协议细节 | **真实缺陷已修（`t03-01-07`）**：`syncReceiptMismatch`（`NS-STORAGE-004`，即「重传的块内容与冻结清单冲突」）**原先没有线路码映射**，于是 `StorageException.toProtocolError()` 走兜底 `?? dbCommitFailed`，把 §8 要求的 `CHUNK_HASH_MISMATCH`（422，不可重试）报成 `DB_COMMIT_FAILED`（500，**可重试**）——**行为上邀请客户端无限重发一个内容冲突的块**，正是 §8/§11 各自禁止的事。现已映射为 `chunkHashMismatch` 并在 `storage_migration_test.dart` 钉住状态码与不可重试性。**残余**：兜底分支本身仍在，`schemaTooNew`/`migrationFailed`/`backupFailed` 这些「never reaches the peer」的本地拒绝仍会以 `DB_COMMIT_FAILED` 报出。**需显式决定**（抛错还是定义一个通用内部码），本批不擅自设定；目前无调用方，属潜在问题 |
| 未知方法答 `NOT_FOUND` | §11 的表**没有 405**；`AGENTS.md` §3 禁止发明协议细节 | **解读（`t03-01-04`）**：采用 §7 已写下的「任务查询对无权限资源统一 404」，同时**不确认哪些端点存在**。**需在协议冻结前确认**：是维持 404，还是为 405 单独定义语义（那将是一次协议增补） |
| 游标必须免转义 | §7 称游标为「返回的不透明值」而**未定格式**；`t03-01-04` 为去掉 `%2F`/`%2e%2e` 绕过，在**整个请求目标**中拒绝百分号编码 | **需人工确认**：这是对留白格式施加的约束（服务端必须签发不含需转义字符的游标）。若将来允许含 `%` 的游标，必须把「整目标拒绝」放宽为「路径段拒绝、查询值允许」——那是一次安全性权衡，不应顺手改 |
| `startIndex`/`limit`/`fileId` 的必填性 | §7 只给出参数名，未说明哪些必填 | **本读法（`t03-01-04`）**：manifest 的 `kind` 必填、`startIndex` 默认 0、`limit` 默认为该端点上限；`status` 的 `fileId` 允许缺省。**需确认** |
| 分页的 1 MiB 约束 | §6：页「同时受 1 MiB 上限约束」 | **已实现（`t03-01-05`）**：分页器先按条目上限贪心填充，再把**候选页编码出来量一次**，超出就收缩——估算需要同时把 JSON 转义、十进制字段与多字节字符都建模对。**诚实的发现**：按 §5.1 的 1024 字节路径上限，现实输入下**条目上限总是先起作用**，字节上限是安全网；测试用最大合法路径填满 128 项断言一页装得下，另用发送方不可能构造出的超大条目驱动机制本身 |
| 暂存是否需要持久化 | §6 未说明 staging 是否必须跨重启保留 | **本读法（`t03-01-05`）**：`ManifestStaging` 是进程内的；服务端重启需客户端重传，而重传是幂等且安全的（§6 的重传语义保证）。**需确认**协议是否要求 staging 持久 |
| 清单的 30 分钟窗口 | §6：首次收到内容后 30 分钟未完成则清理并撤销提议 | **谓词已实现，无执行者（`t03-01-05`）**：`isExpired` 存在且从**首次内容**起算，但**没有任何代码在调用它**，也没有代码执行清理。接线时必须确保定期检查，且清理**不影响已冻结的任务** |
| 「哪些数字是字符串」的逐字段落实 | §4 分两类列出：字节数／块编号／`chunkCount`／`leaseEpoch`／`checkpointSeq` 是**十进制字符串**；协议版本／`chunkSizeBytes`／`length`／`fileCount`／`pageLimit` 是 **JSON 整数** | **已实现并测试（`t03-01-06`）**：`totalBytes`／`leaseEpoch`／`checkpointSeq`／块 `index` 编码为字符串，`fileCount` 编码为数字，互换即拒绝；`encodeDecimalString` 补齐了编码端并拒绝负数与超范围值。**需确认** `totalBytes` 归入「字节数」这一读法——§4 只写类别名，未逐字段点名 |
| §9 缺块区间的线上形状 | §9：`missingChunkRanges` 是「**两端包含的十进制字符串对**」 | **草案留白，故意未实现（`t03-01-06`）**：「对」是二元素数组还是 `{start,end}` 对象未定。`AGENTS.md` §3 禁止以偏好定下未决细节，故本次**不建模该体**，待确认后再实现 |
| `AuthorizationGrant` 两个秘密的长度 | §3 只说访问令牌是 32 字节随机值，未逐字说明这两个字段 | **本读法（`t03-01-06`）**：两者都按 32 字节规范 base64url 校验。若它们的实际长度不同，**需改** |
| 控制命令一页的上限 | §7 未规定一页 `commands` 的数量上限 | **未设（`t03-01-06`）**：本层只校验升序与 `lastSeq` 覆盖，没有条目数上限；现存唯一约束是 §4 的 1 MiB 控制体上限。实现 HTTP 层时需确认是否需要额外上限 |
| `checkpoint_seq` 的落点：`tasks` 还是 `sessions` | `docs/跨平台离线文件互传系统技术方案_V2.1.md` §225 把 `checkpoint_seq` 放在 **`sessions`** 表（与 `lease_epoch`、会话状态、过期时间同表）；架构文档 §8 把它列为**建议**实体并写明「实际 SQL…由独立 schema 设计任务冻结」；协议 §8/§9 **只**要求 `checkpointSeq` 与块标志同事务提交、并与 `leaseEpoch` 一起上报，**未点名表** | **落在 `tasks`（`t03-01-08`，schema v3）**：本仓库**没有 `sessions` 表**，而 `lease_epoch` 实际在 `tasks`（T04-01/ADR-0003），故把与它永远成对上报、成对比较的计数器放在同一行。这是**对已实现 schema 的最小一致改动**，不是对 V2.1 的无视。**需人工决定**：补一张 `sessions` 表把会话概念补上（那会同时牵动 `lease_epoch` 的搬迁），还是接受「`sessions` 表已被 T04-01 的 `tasks` 取代」并把 V2.1 与架构文档改为现状 |
| `checkpointSeq` 的作用域 | 协议 §8 只写「SQLite 事务提交块标志和 `checkpointSeq`」，§9 只写上报 `{leaseEpoch, checkpointSeq, committedBytes}`，**未说明**是每任务还是每文件 | **本读法（`t03-01-08`）**：**每任务**，因为它的搭档 `leaseEpoch` 明确是任务级写入世代，而一个与 `leaseEpoch` 成对上报的值应与它同域。测试固定了「第二个文件取到序号 2 而非从 1 重启」。**需确认**：若协议期望每文件序号，则发送方的「不回退」比对必须改为逐文件，且状态体的 `fileId?` 语义也会不同 |
| 1 秒窗口从哪一刻起算 | 协议 §8「每 16 MiB 或 1 秒 checkpoint」，**未说**这 1 秒从首次待提交、最近一次提交还是最近一次入队起算 | **从第一个待提交块起算（`t03-01-08`）**，并已测试固定。理由：若从最近一次入队起算，一个持续有块到达的接收方可以**无限推迟** checkpoint，1 秒这条界限就永远不生效。**需确认**：对端若按别的起算点理解，双方会在「该不该已经 checkpoint」上分歧 |
| `verified_pending` 是否应当成为第三种块状态 | 协议 §8 有 `verified_pending` 与 `committed` 两种**响应**状态；`AGENTS.md` §2 规则 5 让已提交块成为恢复的唯一权威 | **有意不建第三种 DB 状态（`t03-01-08`）**：`chunks.state` 仍是 `missing`/`committed`，「已写但未提交」不是进度，加一个 `verifying` 状态会成为「我们到哪了」的第二个答案。`verified_pending` 只是响应，所以窗口中途崩溃会把块留在 `missing` 并被重传。测试直接钉住「三个 `verified_pending` 之后 `committedChunkCount == 0` 且 `missingChunkIndices == [0,1,2,3]`」 |
| 批量提交的「验证」与「提交」被分成两次调用 | §8 写入顺序是「写入并验证长度/块摘要→受控待提交队列→`syncData`→事务提交」；原实现把验证与提交放在同一次调用里 | **收据带摘要（`t03-01-08`）**：窗口持有的是 `(index,length,sha256)` **收据**而非索引，批量提交在**事务内**把每张收据与冻结清单再比对一次，冲突则**整批失败**。若只记索引，批量提交就成了一条「把任何已登记块标成已提交而无需证据」的路径。**需人工复核**：这只在调用方真的把 sink 报告的摘要放进收据时才成立，属**代码审查**项，测试无法覆盖「调用方如实填报」 |
| 既有任务在 v3 升级后的 `checkpoint_seq` | 协议 §8 只要求序号**不回退**；未说明升级前就存在的任务应从几开始 | **写 0（`t03-01-08`）**：既有任务在引入计数器之前**确实**没有取过 checkpoint，0 就是「尚未取过」而不是伪造序号；`NOT NULL DEFAULT 0` 让它是**值**而非缺失（测试断言写 `NULL` 被拒）。**需确认**：若协议要求既有任务继承某个非零序号，需改 |
| `kDbSchemaVersion = 1` 与「数据库尚未创建」已与现实不符 | T01-01 任务卡要求「`kAppVersion`、`kProtocolMajor`、`kProtocolMinor`、`kDbSchemaVersion` 各自**只有一个定义处**；其它层只能消费，**不得重复定义**」 | **发现的文档—现实漂移，本批未改（`t03-01-08`）**：`lib/core/build_info/build_info.dart` 的 `kDbSchemaVersion = 1` 与 `kDatabaseImplemented = false` 使「关于」页显示「数据库 schema 版本 1（已声明，数据库尚未创建）」，而 T04-01 已实现 SQLite schema 与迁移、**本次之后 `currentVersion = 3`**——**数值与措辞都不再成立**，且已有**两处** schema 版本定义（另一处 `StorageSchema.currentVersion`），正是 T01-01 禁止的重复定义。**不擅自改**，因为需先定 `kDatabaseImplemented` 的语义：「存储层已实现」还是「运行中的应用真的会打开数据库」（目前 `NearSendApp` 未打开）。两个候选：①**派生**——删除 `kDbSchemaVersion`，`dbSchemaDisplay` 消费 `StorageSchema.currentVersion`，并把「尚未创建」改为按实际装配状态报告；②**保留声明值**但改名与改文案，明确它描述的是应用装配而非代码存在。台账 §6 的对应描述（§3 与 §7 记录）需随之更新 |
| 平台目录边界 | `android`/`windows`/`ios` 相对 `flutter create` 生成结果只允许标识类差异，不得混入业务逻辑 | T01-01 已按 SHA-256 逐文件审计通过，见 [平台目录审计](testing/evidence/2026-09-20/t01-01-01/platform-directory-audit.md)；后续每个平台任务需重复复核 |
| 依赖锁定 | `pubspec.lock` 必须入库；新增依赖需按端侧设计 §12 记录用途、许可证、维护状态与安全影响 | T01-01 已修正 `*.lock` 误忽略并跟踪 `pubspec.lock`；T01-01 未引入任何第三方运行时依赖 |
| 应用标识 | `applicationId` / bundle identifier 发布后不可更改 | 已在 [ADR-0001](decisions/ADR-0001-工程基线与标识.md) 冻结为 `com.nearsend.app`；变更必须在 T10 之前完成 |
| CI 门禁范围 | 自动化门禁只覆盖静态检查、单元/组件测试与构建；**不得**把它当作真机、网络、耐久性或安全结论的替代 | T01-02 已建立并在真实运行中通过，见 [运行汇总](testing/evidence/2026-09-20/t01-02-01/summary.md)；平台能力结论仍必须由目标设备证据支持 |
| CI 工具链与供应链 | Action 必须固定到提交 SHA、权限只读、禁止 `continue-on-error`；Flutter 版本与安装脚本两处必须一致 | 已由 `tooling/checks/check_ci_workflow.py` 在 CI 中强制 |

这些项阻止产品发布，不妨碍将明确标注为实验的源码和证据归档入仓。

## 6. 本次仓库交付与验证

- 新增 UI Baseline 1.0：精确 Design Token、组件规格、移动端四屏核心流程和 Windows 双栏传输页；SVG 已实际渲染检查。该状态只表示视觉设计完成，不表示 Flutter 页面已实现。
- 新增文档中心、Vibe Coding 流程、系统架构、应用与端侧服务、UI/UX、质量策略和 Agent 任务手册，并在 README 建立入口。
- 文档明确 NearSend 无中心云后台；“后台”是每台设备内嵌的本地 HTTPS 服务、传输引擎、SQLite 与平台适配。
- 建立需求/规格 → 任务 ID → 分支/PR → 测试证据 → 台账状态的追踪链，并拆出首批可领取子任务。
- 本次为文档与治理交付，未创建 Flutter 工程，也未改变任何真机或产品能力状态。
- 将协议和固定向量放入docs/protocol/；报告与原始证据放入docs/testing/；参考代码放入tooling/s0/。
- 仅修改向量路径等仓库集成细节，没有替换现有技术方案的产品摘要、发布渠道或AI规则。
- 更新README状态与文档入口；本台账是项目状态入口。
- 导入后执行 `cd tooling/s0 && python3 -m unittest -v test_probes`：24项通过，日志独立保存。
- 使用Python语法解析、相对文档链接检查、证据摘要校验和敏感文件检查验证导入。
- 未运行Flutter格式化/analyze/test：没有Flutter工程和SDK；没有Python格式化工具配置，本次不引入新工具或全量格式化。
- 未重复20GiB和小文件高成本实验：源码算法未变，历史原始证据完整保留。
- 未完成三端真机、真实断电、完整HTTP接口、生产背压、导出及迁移验证，详见阻塞表。

### 6.1 T01-01 Flutter 工程基线（本次交付）

任务卡：[T01-01](tasks/T01-01.md)　决策：[ADR-0001 工程基线与标识](decisions/ADR-0001-工程基线与标识.md)　
证据：[运行汇总](testing/evidence/2026-09-20/t01-01-01/summary.md)（运行 ID `t01-01-01`）

- 首次创建 Flutter 产品工程：`android`、`windows`、`ios` 三端平台目录，`lib/` 按
  `app / core / features` 分层，`test/` 覆盖构建信息、设计 Token、首页与版本信息页。
- 应用为**版本可追踪的壳**：显示应用版本、Git 提交（构建期 `--dart-define` 注入）、协议版本
  （明确标注 `1.0-draft1` 草案未冻结）与数据库 schema 版本（明确标注「已声明，数据库尚未创建」）。
  首页两个主操作**显式禁用并标注未实现**，未伪造任何业务能力。
- 新增 `tooling/scripts/check.ps1` 与 `build.ps1`：把 `AGENTS.md` §7 的必做检查与
  Android/Windows 构建固定为一组可复现命令，并把 Git 提交注入产物、输出产物 SHA-256。
- 新增 `docs/tasks/`（任务卡唯一存放位置，替代 GitHub Issue，理由见该目录 README）
  与 `docs/decisions/`（ADR-0001）。
- 修正根 `.gitignore` 中 `*.lock` 误忽略 `pubspec.lock` 的问题，满足 `AGENTS.md` §3 依赖锁定要求。
- 本机原先没有 Flutter/Dart SDK，`C:\tools\Android` 为空；本次安装 Flutter 3.47.5、
  Android SDK（platform-36、build-tools 36.0.0、NDK 28.2.13676358、platform-tools 37.0.1）
  并写入用户级环境变量。安装事实与脚本摘要记入 [environment.json](testing/evidence/2026-09-20/t01-01-01/environment.json)。
- 验证结果：`dart format` 无改动、`flutter analyze` 无问题、`flutter test` 29/29 通过、
  `tooling/s0` 24/24 通过；Android debug/release APK 与 Windows release 可执行文件均构建成功；
  release APK 在 **Xiaomi 25102RKBEC / Android 17 (API 37)** 真机安装并启动，
  版本信息页四项数值与构建提交一致；Windows 10 22H2 上可执行文件启动、窗口标题为 `NearSend`、
  键盘 `Tab`+`Enter` 可进入版本信息页。
- 平台目录经 SHA-256 逐文件审计，相对 `flutter create` 原始生成结果**只有标识类差异**，
  没有业务逻辑进入平台目录，见 [平台目录审计](testing/evidence/2026-09-20/t01-01-01/platform-directory-audit.md)。
- **附带修复**：`tooling/s0` 原先用不带编码的 `read_text()`/`write_text()` 处理 UTF-8 协议向量，
  在本机 `cp936` locale 下不可复现（实际 21 项运行、1 项失败）。已显式指定 UTF-8，并验证
  在 cp936 机器上重新生成向量得到字节相同的文件。原始失败日志保留未覆盖，见
  [S0 探针 Windows 复现](testing/evidence/2026-09-20/t01-01-01/s0-probe-baseline-on-windows.md)。
- **并入 UI Baseline 1.0**：本任务期间 PR #3 合并了 `docs/ui/STYLE_GUIDE.md` 并使其成为视觉权威。
  该指南推翻了原实现采用的两处数值（卡片圆角 12→**16**、按钮圆角 10→**12**），并新增
  `canvas`/`border`/`textMuted`/`primaryHover`/`primarySoft` 与三个语义 soft 背景、七级精确排版比例。
  `lib/app/theme/design_tokens.dart` 已按指南重写（`NearSendPalette` → `NearSendColors`），
  Flutter 测试由 29 项增至 36 项，并新增 canvas/card 区分、卡片 1dp 边框与语义色对比度检查。
  按 `AGENTS.md`「合并前处理基线变化并重新运行相关检查」，`origin/master` 已并入分支并解决
  §7 冲突后完整重跑全部检查与两端构建。
- 未执行/未完成：iOS 构建（本机无 macOS/Xcode，B06/T09 仍阻塞）；Windows 11 验证（本机为 Win10）；
  正式签名（T10）；深色模式、动态字体、屏幕阅读器与一万项列表性能（后续 UI 任务）；
  以及全部传输、配对、发现、存储与恢复能力（尚未实现）。

### 6.2 T01-02 CI 与检查（本次交付）

任务卡：[T01-02](tasks/T01-02.md)　证据：[运行汇总](testing/evidence/2026-09-20/t01-02-01/summary.md)（运行 ID `t01-02-01`）

- 新增 `.github/workflows/ci.yml`：四个作业（仓库检查、format/analyze/test、Android 构建、
  Windows 构建）。**没有任何 `continue-on-error`**，工作流权限为只读；
  所有 `uses:` 固定到 40 位提交 SHA，只使用 GitHub 官方一方 Action（MIT）。
- Flutter 工具链由 `tooling/ci/install_flutter.sh` 安装：版本固定 3.47.5 并**校验归档 SHA-256**
  （Linux `2132e990…`、Windows `0ccd7193…`，后者在 T01-01 中由本地实际下载独立核对）。
  不使用第三方 Action 安装 Flutter，理由与依赖评估记入运行汇总 §2。
- 新增 `tooling/checks/` 三个**仅用 Python 标准库**的检查：相对链接、敏感信息、CI 工作流不变量。
  `check.ps1` 与工作流调用同一份脚本，因此本地与 CI 执行同一组规则，不存在两套会分叉的实现。
- `build.ps1` 改为跨平台（搜索路径使用正斜杠），使 ubuntu 作业运行开发者本地使用的同一份构建逻辑，
  而不是在 YAML 里再写一遍。
- 新增 `.gitattributes` 声明行尾与二进制策略；`git add --renormalize .` 未改变任何既有文件内容。
- **真实 CI 结果**：首次运行（run 35521877748）3/4 作业通过，`Markdown relative links` 失败；
  修复后运行（run 35522211411，提交 `eb8c4d9`）**四个作业全部 success**。
  首次失败暴露了两个真实问题并已修复：检查器把文档中的行内代码示例当成真实链接；
  以及本地的「通过」只覆盖 26 个 Markdown 而 CI 看到 29 个（差额是当时尚未 `git add` 的新文档）。
  现在检查器会跳过代码块/行内代码/HTML 注释，并显式列出未跟踪的 Markdown 打印 `NOT CHECKED`。
  **首次失败的完整日志保留未覆盖**，见 `ci-run-35521877748-repository-checks-FAILED.log`。
- 反例验证：5 类缺陷（坏链接、假令牌、标签固定 Action、`continue-on-error`、Flutter 版本漂移）
  全部被对应检查抓到；敏感信息扫描的输出经过脱敏（只打印前 4 字符与长度）。
- 未执行/未完成：签名与发布作业（T10）；macOS runner 与 iOS 构建（B06/T09 仍阻塞）；
  pub/Gradle 依赖缓存优化；夜间集成测试与真机证据（按质量策略由里程碑任务承担）。

### 6.3 T02-01 Dart canonical manifest（本次交付）

任务卡：[T02-01](tasks/T02-01.md)　决策：[ADR-0002 crypto 依赖](decisions/ADR-0002-crypto依赖与SHA256.md)　
证据：[运行汇总](testing/evidence/2026-09-20/t02-01-01/summary.md)（运行 ID `t02-01-01`）

- 在 `lib/core/protocol/` 中**依据规范文本**实现 `LFTM1`（清单摘要）与 `LFTC1`（块清单摘要）：
  大端定宽写入器、§4 的十进制字符串/整数/UUID/摘要校验、§5.1 的相对路径词法规则、
  以及由文件大小与索引推导块长度与偏移的算术。
- **未逐行翻译 Python 参考实现**：编码器按协议 §5.2/§5.3 的字节布局描述编写，
  固定向量作为对规范理解的**外部检验**。三个向量（空文件、UTF-8 中文路径、4 MiB+3 字节尾块）
  的 `canonicalHex`、`manifestDigest`、`chunkManifestDigest` 全部逐字节一致。
- 显式断言 `manifestDigest != SHA-256(manifest JSON)`，证明没有走 §5.2 禁止的「哈希 JSON 文本」捷径；
  并验证 JSON 键序与空白不影响摘要、`files` 数组重排影响摘要。
- 反例覆盖：十进制非法形式、路径穿越/绝对路径/保留设备名/控制字符/孤立代理项/超过 1024 字节、
  未知与缺失字段、重复 `fileId`、超出 10,000 文件与 1,048,576 块、块缺失/重复/乱序/
  非末块短块、块长度与文件大小不符、摘要不匹配。
- 20 GiB 规模下块数 5120、高索引偏移大于 0xFFFFFFFF，验证 64 位算术；越界索引被拒绝而非回绕。
- 过程中发现并修复一个真实缺陷：路径校验把 `/` 误列为非法字符，而它是协议唯一允许的分隔符，
  导致任何带目录的路径（含中文向量）被拒绝。**只有真正跑固定向量才会暴露**，已修复并保留全部反例。
- 新增依赖 `crypto` 3.0.7，按端侧设计 §12 的九项要求记录为 ADR-0002；自制 SHA-256 被明确否决。
- **已知缺口**：协议 §5.1 要求的非 NFC 路径拒绝**未实现**，已登记在 §5 并需在协议冻结前解决。
- 未执行/未完成：完整错误模型与版本协商（T02-02）；原生（Kotlin/Swift/Windows）独立实现比对；
  流式哈希调度与读取用户文件（T03/T04）。

### 6.4 T02-02 状态、错误码和版本协商（本次交付）

任务卡：[T02-02](tasks/T02-02.md)　证据：[运行汇总](testing/evidence/2026-09-20/t02-02-01/summary.md)（运行 ID `t02-02-01`）

- 新增协议模型：`protocol_version.dart`（版本、能力与协商算法、冻结任务不可变判定）、
  `transfer_state.dart`（任务与文件状态机）、`retry_policy.dart`（超时、退避与抖动、Retry-After 上限）、
  `idempotency.dart`（`request_id` 幂等、`LeaseEpoch`/`LeaseGuard`、`ResumeCoordinator`）；
  并扩展 `protocol_exception.dart` 为 §11 的完整错误模型。
- 错误模型把「能否重试」当作线上契约（§7 的错误体含 `retryable`）：**只有** §11 要求退避的
  `RATE_LIMITED`/`STORAGE_SYNC_FAILED`/`DB_COMMIT_FAILED` 可自动重试；
  `SPACE_INSUFFICIENT` 明确不可自动重试，因为它需要用户先采取行动。
- 状态机的每条迁移都标注来源（`stated`/`derived`）并在 rationale 中引用 §10，
  测试断言「每条迁移都有依据」，使表格无法悄悄扩张。
- `ResumeCoordinator` 固定 §9 的顺序：同 ID 不同参数 → 冲突；在途 → 202 语义；
  已完成 → 重放且**不再升代**；已有更高世代 → `STALE_RESUME_REQUEST`；否则分配新世代。
- 新增 79 项协议测试（48 → 127），Flutter 测试总数 84 → 163。
- **发现并登记三处草案缺口**（见 §5）：能力词表未定义；`BLOCKED`/`FAILED`/`PARTIALLY_COMPLETED`
  无出边，其中 `blocked` 只能取消、与 §11 的处方冲突；文件级 `failed` 无出边，无法「只重试失败项」。
  三者都以可执行断言固定，属**协议变更**，冻结前必须解决。
- 未执行/未完成：协议冻结本身；HTTP 服务器与路由（T03-01）；幂等与世代的持久化（T04-01，
  必须与效果同事务提交）；能力词表取值；原生实现比对。

## 7. 更新记录与维护规则

| 日期 | 变更 | 证据 |
| --- | --- | --- |
| 2026-09-20 | 产品方向、开发顺序和严格校验取舍确认 | 技术方案V2.1 |
| 2026-09-20 | 完成Linux参考实验及协议draft1 | 原始日志、JSON结果和报告 |
| 2026-09-20 | 导入仓库、完成24项复测、建立本台账 | 本次Git提交/PR、repository-import-tests.log |
| 2026-09-20 | 建立 Vibe Coding 研发流程、实施架构、端侧/UI/质量规格和 Agent 任务手册 | D06–D12、本次文档 PR |
| 2026-09-20 | 完成 UI Baseline 1.0、组件样式和移动端/Windows视觉稿 | D10、UI设计PR |
| 2026-09-20 | T01-01 完成：创建三端 Flutter 工程与版本壳应用，Android 真机与 Windows 构建/运行通过，建立任务卡与 ADR 目录，修正 `pubspec.lock` 与 S0 探针编码缺陷 | [T01-01](tasks/T01-01.md)、[运行汇总](testing/evidence/2026-09-20/t01-01-01/summary.md)、[PR #4](https://github.com/yanzhao77/NearSend/pull/4) |
| 2026-09-20 | 修正 T02-01/T02-02 依赖列（实际依赖 T01-01 工程），B01–B04 阻塞口径按新环境更新 | 本台账 §3、§4.1 |
| 2026-09-20 | T01-01 并入 UI Baseline 1.0：设计 Token 按 `docs/ui/STYLE_GUIDE.md` 重写（圆角 16/12、新增 canvas/border/muted/soft 与精确排版比例） | [样式指南](ui/STYLE_GUIDE.md)、[PR #4](https://github.com/yanzhao77/NearSend/pull/4) |
| 2026-09-20 | T01-01 经 PR #4 合并入 `master`（合并提交 `5a3d840`），任务状态由「已完成（待合并）」转为「已完成」 | [PR #4](https://github.com/yanzhao77/NearSend/pull/4)、合并提交 `5a3d840` |
| 2026-09-20 | 构建脚本修订：产物摘要排除本次运行自身重写的证据日志，使干净提交的构建立即体现为 `gitDirty=false`；修复该判定在 `Set-StrictMode` 下对单个变更路径报错的问题。证据按新的干净修订重新采集 | [build.ps1](../tooling/scripts/build.ps1)、T01-01 证据 |
| 2026-09-20 | T01-02 完成：建立 GitHub Actions 门禁（仓库检查、format/analyze/test、Android/Windows 构建），固定并校验 Flutter 工具链摘要，新增三项跨平台仓库检查与 `.gitattributes`；真实 CI 首次失败后修复并全部通过 | [T01-02](tasks/T01-02.md)、[运行汇总](testing/evidence/2026-09-20/t01-02-01/summary.md)、[PR #6](https://github.com/yanzhao77/NearSend/pull/6) |
| 2026-09-20 | 记录一条过程教训：仓库级检查只扫描已跟踪文件，因此**本地通过不代表覆盖工作区**。检查器现在会列出未跟踪的 Markdown 并打印 `NOT CHECKED` | T01-02 运行汇总 §6、`ci-run-35521877748-repository-checks-FAILED.log` |
| 2026-09-20 | T02-01 待验证：Dart 独立实现 LFTM1/LFTC1，三个固定向量逐字节一致；新增 `crypto` 依赖（ADR-0002）；登记非 NFC 路径强制缺口 | [T02-01](tasks/T02-01.md)、[运行汇总](testing/evidence/2026-09-20/t02-01-01/summary.md)、[ADR-0002](decisions/ADR-0002-crypto依赖与SHA256.md) |
| 2026-09-20 | D04 由「待 Dart/原生独立比对」更新为「已有 Dart 独立比对」；T02 说明补充 Dart 实现已一致 | 本台账 §2、§4 |
| 2026-09-20 | T01-02 经 PR #6（合并提交 `9d784a4`）与 PR #7（合并提交 `11d4304`）合并入 `master`；CI 在 `master` 上同样全部通过（run 35522441321） | [PR #6](https://github.com/yanzhao77/NearSend/pull/6)、[PR #7](https://github.com/yanzhao77/NearSend/pull/7) |
| 2026-09-20 | T02-01 经 PR #8 合并入 `master`（合并提交 `214f519`），任务状态由「待验证」转为「已完成」 | [PR #8](https://github.com/yanzhao77/NearSend/pull/8)、合并提交 `214f519` |
| 2026-09-20 | T02-02 待验证：错误模型、状态机、版本/能力协商、超时重试、`request_id` 幂等与 `lease_epoch` 收敛为可测试模型（协议测试 48→127，总计 163） | [T02-02](tasks/T02-02.md)、[运行汇总](testing/evidence/2026-09-20/t02-02-01/summary.md) |
| 2026-09-20 | **发现三处协议草案缺口并登记**：能力词表未定义；`BLOCKED`/`FAILED`/`PARTIALLY_COMPLETED` 无出边（`blocked` 只能取消，与 §11 处方冲突）；文件级 `failed` 无出边，无法「只重试失败项」。三项均属协议变更，**冻结前必须解决** | 本台账 §5、T02-02 运行汇总 §4 |
| 2026-09-20 | T02-02 经 PR #10 合并入 `master`（合并提交 `29ed41c`），任务状态由「待验证」转为「已完成」；CI 四个作业在首次运行即全部通过（run 35525798037） | [PR #10](https://github.com/yanzhao77/NearSend/pull/10)、合并提交 `29ed41c`、T02-02 证据 |
| 2026-09-20 | T04-01 由「待澄清」转为「就绪」：范围写入任务卡；SQLite 绑定按 §12 完成候选评估与实测验证（ADR-0003），并移除已废弃且为空的 `sqlite3_flutter_libs`。schema/迁移/repository 实现尚未开始 | [T04-01](tasks/T04-01.md)、[ADR-0003](decisions/ADR-0003-SQLite绑定与耐久性配置.md)、[绑定验证证据](testing/evidence/2026-09-20/t04-01-01/summary.md) |
| 2026-09-20 | T04-01 转为「进行中」：实现 schema v1、迁移框架与一致性备份、高版本拒绝、chunk repository（提交顺序、缺失块权威、写入世代仲裁）及 38 项测试（Flutter 测试 168→201）。**未完成**：幂等持久化、peers/exports 仓储、ENOSPC 注入、真实暂存损坏、多版本迁移、真机 syncData | [T04-01](tasks/T04-01.md)、[实现证据](testing/evidence/2026-09-20/t04-01-02/summary.md) |
| 2026-09-20 | T04-01 存储核心经 PR #13 合并入 `master`（合并提交 `df0481a`）；CI 四个作业首次运行即全部通过（run 35527238578）。任务**保持「进行中」**，退出门槛未满足 | [PR #13](https://github.com/yanzhao77/NearSend/pull/13)、合并提交 `df0481a`、T04-01 证据 |

| 2026-09-20 | T04-01 幂等持久化：实现 `idempotency_repository.dart`，**记录与效果在同一事务提交**（以「效果写标记行 + 断言两者同时提交/同时回滚」直接验证），规则复用 T02-02 定义；Flutter 测试 201→214。任务仍为「进行中」 | [T04-01](tasks/T04-01.md)、[实现证据](testing/evidence/2026-09-20/t04-01-03/summary.md) |
| 2026-09-20 | T04-01 幂等持久化经 PR #15 合并入 `master`（合并提交 `2d964cb`）；CI 四个作业首次运行即全部通过（run 35527991092）。任务**保持「进行中」** | [PR #15](https://github.com/yanzhao77/NearSend/pull/15)、合并提交 `2d964cb`、T04-01 证据 |
| 2026-09-20 | T04-01 任务/文件状态、导出记录与 peer 授权：实现 `transfer_repository.dart`、`peer_repository.dart` 及 22 项测试（Flutter 测试 214→236；存储测试 51→73）。状态迁移只走 T02-02 的已定义边；导出仅在文件全部 committed 后才记录且不静默产生第二份副本；peer 指纹变化只报告不吸收。**期间修复**：`NearSendDatabase.transaction` 把状态机抛出的 `ProtocolViolation` 包装成 `NS-STORAGE-*`，使「协议拒绝」被报告为「磁盘失败」，已改为原样重抛。任务仍为「进行中」 | [T04-01](tasks/T04-01.md)、[实现证据](testing/evidence/2026-09-20/t04-01-04/summary.md) |
| 2026-09-20 | T04-01 上述实现经 PR #17 合并入 `master`（合并提交 `751b8a9`）；CI 四个作业在首次运行即全部通过（run 35528942547，head SHA `50810f1`）。任务**保持「进行中」**，退出门槛未满足 | [PR #17](https://github.com/yanzhao77/NearSend/pull/17)、合并提交 `751b8a9`、T04-01 证据 |
| 2026-09-20 | T04-01 磁盘满与提交失败注入：以 `PRAGMA max_page_count` 让**引擎真实返回** `SQLITE_FULL`（result code 13），新增 8 项注入测试（Flutter 测试 236→244；存储 73→81）。探针给出一个否定结论：块提交是单行 `UPDATE`、不分配新页，**数据库写满也无法让它失败**，故注入位置在 sink，数据库侧另经事务原语验证。**期间修复**：真实的空间耗尽原先被包装为 `NS-STORAGE-006 commitFailed` → `DB_COMMIT_FAILED`（500，**可重试**），会让客户端对着写不进去的卷无限重试、且永不提示用户清理空间；已新增 `NS-STORAGE-008 spaceInsufficient` → `SPACE_INSUFFICIENT`（507，**不可重试**）。任务仍为「进行中」 | [T04-01](tasks/T04-01.md)、[实现证据](testing/evidence/2026-09-20/t04-01-05/summary.md) |
| 2026-09-20 | T04-01 上述修复与注入测试经 PR #19 合并入 `master`（合并提交 `cf91e9e`）；CI 四个作业在首次运行即全部通过（run 35529727942，head SHA `d02a0c7`）。任务**保持「进行中」**，退出门槛未满足 | [PR #19](https://github.com/yanzhao77/NearSend/pull/19)、合并提交 `cf91e9e`、T04-01 证据 |
| 2026-09-20 | 新建 [T06-01](tasks/T06-01.md) 任务卡并转为「进行中」：空间计划、终检与导出。**第一批 `t06-01-01` 完成空间计划**——实现 `space_plan.dart` 与 36 项测试（Flutter 测试 244→280；存储 81→117）。按 V2.1 §16.2 与端侧设计 §8 逐条对应：按卷核算、五个明细项相加等于总需求（有断言）、已分配暂存不重复扣减、安全余量按卷取 `max(256 MiB, 1%)`。最该保留的不变量是 **`unknown ≠ sufficient`**：判定先判空再比较，不存在把缺失读数当作通过的路径。**期间修正**：数据库估算常数由测试发现偏小（实测 200 块增长 53,248 B，初版仅估算 52,224 B），已提高到 320 B/块并在两个块数上验证。终检、导出与清理未完成 | [T06-01](tasks/T06-01.md)、[实现证据](testing/evidence/2026-09-20/t06-01-01/summary.md) |
| 2026-09-20 | **修正三处文档不一致**（`docs/tasks/README.md` 要求任务卡状态与台账一致，实际不一致）：`T01-02` 卡由「就绪 → 进行中」改为「已完成」，`T04-01` 卡由「就绪」改为「进行中」，任务卡索引由只列 1 张改为列全 6 张（含状态与 PR）。修正前用 `git log --merges` 逐个核对合并提交确实存在，避免用一份可能同样过期的文档去「修正」另一份 | [任务卡索引](tasks/README.md)、[T01-02](tasks/T01-02.md)、[T04-01](tasks/T04-01.md) |
| 2026-09-20 | T06-01 空间计划经 PR #21 合并入 `master`（合并提交 `86a0058`）；CI 四个作业在首次运行即全部通过（run 35530499681，head SHA `40b18e6`）。任务**保持「进行中」**，终检、导出与清理未完成 | [PR #21](https://github.com/yanzhao77/NearSend/pull/21)、合并提交 `86a0058`、T06-01 证据 |
| 2026-09-20 | T06-01 **第二批 `t06-01-02` 完成整文件终检**——实现 `file_verification.dart` 与 22 项测试（Flutter 测试 280→302；存储 117→139）。**一次流式读取同时回答两个问题**（整文件摘要＝导出闸门；分块摘要＝修哪一块），字节以流消费、唯一增长的是 SHA-256 状态。`wholeFileDigestMatches` 为 `bool?`：`null` 表示**没有计算**（有块缺失/损坏时摘要算在错误数据上），且 `null` 与 `false` 一样阻断导出。损坏块降级为 `missing`（协议 §5 允许 `committedBytes` 下降，因为内容错误的 committed 块会被之后每次恢复信任）；跨模块验证：终检降级后 `isFullyCommitted` 为假、`recordSavedExport` **自己**也拒绝，两个组件各自独立拒绝「假完成」。**新登记缺口**：`recordSavedExport` 不要求先通过终检（见 §5）。导出与清理未完成 | [T06-01](tasks/T06-01.md)、[实现证据](testing/evidence/2026-09-20/t06-01-02/summary.md) |
| 2026-09-20 | T06-01 整文件终检经 PR #23 合并入 `master`（合并提交 `d32edf3`）；CI 四个作业在首次运行即全部通过（run 35531324956，head SHA `ab49939`）。任务**保持「进行中」**，导出与清理未完成 | [PR #23](https://github.com/yanzhao77/NearSend/pull/23)、合并提交 `d32edf3`、T06-01 证据 |
| 2026-09-20 | T06-01 **第三批 `t06-01-03` 关闭上一轮登记的缺口并实现安全命名**（Flutter 测试 302→331；存储 139→168）。`recordSavedExport` 的 `verification` 改为**必填**，并校验回执属于该文件、可导出、且覆盖的字节数等于文件**当前**大小；「完成＝终检通过且导出已提交」由代码而非调用约定强制。测试不再拼假回执，而是让暂存字节经 `_StagedMapReader` 真的跑一遍终检，覆盖 `提交 → 终检 → 记录导出` 全链条；新增门禁用例包含「所有块 committed 但整文件摘要不一致」——旧块数检查看不见的情况。`export_naming.dart`：默认大小写不敏感（两种错误不对称：多一个 `(1)` 无害 / 覆盖用户文件不可恢复）、按 rune 截断（切进代理对会得到 U+FFFD，与实际返回的名字不同）、截断撞上保留设备名时加保护前缀（复用 `RelativePathRules.isReservedName` 以保持一份定义）。**期间修正**：初版在截断留下尾随空格/点时跳过整个文件，改为修剪；一处测试前提写错由测试自身暴露。**残余**：回执构造函数公开故不防御应用内伪造；`takenPaths` 枚举不全会覆盖用户文件；导出编排与清理未做 | [T06-01](tasks/T06-01.md)、[实现证据](testing/evidence/2026-09-20/t06-01-03/summary.md) |
| 2026-09-20 | T06-01 导出门禁与安全命名经 PR #25 合并入 `master`（合并提交 `cc7c407`）；CI 四个作业在首次运行即全部通过（run 35532312929，head SHA `351c7dd`）。任务**保持「进行中」**，导出编排与清理未完成 | [PR #25](https://github.com/yanzhao77/NearSend/pull/25)、合并提交 `cc7c407`、T06-01 证据 |
| 2026-09-20 | T06-01 **第四批 `t06-01-04` 完成导出编排、清理与 schema v2**（Flutter 测试 331→358；存储 168→195）。**schema v2**：`exports` 增加 `saved_path`——落盘名字可能与冻结路径不同（命名策略会改名），缺少它就无法回答「文件在哪」、也无法判断目标上的同名条目是不是自己上次写的。`applyVersion1` **一字未改**（现场库已跑过它）。**这也第一次让迁移框架有了真实升级路径**：新增 1→2 端到端 6 项测试，含「新库与升级库的列完全一致」；升级测试最初想截断注册表造 v1 库，被 `assertWellFormed` **正确拒绝**，改为直接调用 `applyVersion1` 重建，**没有为测试便利放宽守卫**——**因此 T04-01 的「多版本迁移」剩余项解除**。**顺序即契约**：清点→命名→提交→**记录**→释放暂存；假 sink 在删除被调用**那一刻**断言记录已存在（暂存是唯一还能产出用户文件的东西）；记录失败则暂存保留；清理失败不改变导出结果。**端口没有删除目标文件的能力**（`inventory`/`commit`/`deleteStaging`），以结构性方式保证「取消不得删除已导出文件」。**「无法证明」窗口**：写入前若自然名字下已有**摘要相同**的条目则复用而非重写（长度相同不算身份证明）。**原子性报告而非声称**（V2.1 §15）。**残余**：平台 `ExportSink` 未实现、取消路径未实现、清点与提交间竞态未处理、提供者给不出摘要时仍可能多一个 `(1)` 副本 | [T06-01](tasks/T06-01.md)、[实现证据](testing/evidence/2026-09-20/t06-01-04/summary.md) |
| 2026-09-20 | T06-01 导出编排、清理与 schema v2 经 PR #27 合并入 `master`（合并提交 `ef00f58`）；CI 四个作业在首次运行即全部通过（run 35533211546，head SHA `887e201`）。任务**保持「进行中」**，平台端口、取消路径与调度接入未完成 | [PR #27](https://github.com/yanzhao77/NearSend/pull/27)、合并提交 `ef00f58`、T06-01 证据 |
| 2026-09-20 | 新建 [T03-01](tasks/T03-01.md) 任务卡，状态由「阻塞」改为「进行中」：网络绑定部分仍阻塞于 B02，但配对逻辑本身未被阻塞，且是 B02 无法替代的部分。**第一批 `t03-01-01` 完成配对载荷与信任上下文**——实现 `pairing_payload.dart`、`pairing_trust.dart`、`base64url.dart` 与 68 项测试（Flutter 测试 358→426），`lib/core/security/` 目录由此建立。**「pin 先于令牌」做成结构性而非约定性**：令牌不是一个字段，只能经 `PairingHandshake.pairingToken` 取得，该 getter 在比对成功前抛 `PAIR_REJECTED`——**写错这条规则的实现仍能与诚实服务端正常工作**，所以不能靠文档约束。不变量收紧为「除非比对显式返回 trusted，否则关闭」，连证书编码不可用也关闭。**指纹三种写法**（DER / PEM 文本 / SPKI）形似而意不同，参数类型定为字节（无 PEM 重载），并有测试断言它与 PEM 文本摘要、与对 DER 一部分的摘要都不同。**载荷严格解析**：未定义字段拒绝（§4）、大写指纹拒绝、base64url 必须规范（同一令牌两种拼写是凭证最不该有的性质）、IPv4 前置零拒绝（部分解析器按八进制）、host 带 scheme/凭证/路径/查询拒绝。**残余**：TLS 引擎、令牌服务端生命周期、`POST /v1/pair`、网络绑定（B02）均未做 | [T03-01](tasks/T03-01.md)、[实现证据](testing/evidence/2026-09-20/t03-01-01/summary.md) |
| 2026-09-20 | T03-01 配对载荷与信任上下文经 PR #29 合并入 `master`（合并提交 `76fd5da`）；CI 四个作业在首次运行即全部通过（run 35534225169，head SHA `fa67e6e`）。任务**保持「进行中」**，TLS 引擎、令牌生命周期、`POST /v1/pair` 与网络绑定（B02）未完成 | [PR #29](https://github.com/yanzhao77/NearSend/pull/29)、合并提交 `76fd5da`、T03-01 证据 |
| 2026-09-20 | T03-01 **第二批 `t03-01-02` 完成令牌生命周期与 `/v1/pair` 模型**（Flutter 测试 426→486；安全层 68→128）。**300 秒窗口以单调计时判定**（墙上时钟可被 NTP 或用户改时间移动，会延长令牌寿命或让活令牌立刻过期）；**服务重启失效是构造性的**——不持久化任何令牌状态，没有「可能被漏掉的失效步骤」；单次消费；响应丢失后无 API 可恢复。**只保存令牌摘要**（§2 已为恢复密钥确立该原则），并**明确标注这一条无测试覆盖**（Flutter 无 `dart:mirrors`），需以代码审查确认。**比较不声称常数时间**：Dart 的 JIT 可重排、对普通比较无时序契约，手写循环会构成**虚假保证**；真正约束风险的是 256 位熵、单次消费、300 秒窗口、滑动限流与只比较摘要。**统一失败做成结构性**：三种拒绝原因映射到同一个常量 `wireCode`，本地理由从线上答案不可达。**限流在令牌之前检查**，已被限流的调用方即使拿正确令牌也只得 429，无法靠换令牌探测存在性。**capability 词表不发明**，复用 T02-02 的 `CapabilitySet`。**新登记张力**：§9 的 `requestId` 幂等与 §3「不恢复已消费令牌」冲突，配对是 §9 的例外，本批未实现任何重放语义（见 §5）。**残余**：HTTP 传输层、会话令牌交付、TLS 引擎、网络绑定（B02）未做 | [T03-01](tasks/T03-01.md)、[实现证据](testing/evidence/2026-09-20/t03-01-02/summary.md) |
| 2026-09-20 | T03-01 令牌生命周期与 `/v1/pair` 模型经 PR #31 合并入 `master`（合并提交 `8e1e211`）；CI 四个作业在首次运行即全部通过（run 35535083875，head SHA `fec4d4a`）。任务**保持「进行中」**，HTTP 传输层、会话令牌交付、TLS 引擎与网络绑定（B02）未完成 | [PR #31](https://github.com/yanzhao77/NearSend/pull/31)、合并提交 `8e1e211`、T03-01 证据 |
| 2026-09-20 | T03-01 **第三批 `t03-01-03` 完成 `/v1` 线上契约层**（Flutter 测试 486→565；协议层 124→204）。**控制体不能只交给 `jsonDecode`**：Dart 的解码器**不拒绝重复对象键**（保留最后一个且不报错），这正是**解析器差分**的形状——两个实现读同一批字节得出不同值；故先扫描结构（深度／重复键／转义）再取值，扫描器**刻意不构建值**。**重复键可以藏在转义里**（`{"a":1,"\u0061":2}`），所以先解码转义再比较（含代理对）；重复键报错**不回显键名**（键来自请求体，控制体可携带令牌）。**错误体 `message` 不是自由文本**：`WireError.of(code)` 不接受 message 参数，只写 `messageKey`，把 T02-02 那句注释变成结构；收到的 message 只限长查控制字符、**不**猜测是否含密钥。**`retryable` 是校验而非采信**（谎称 `CHUNK_HASH_MISMATCH` 可重试等于邀请客户端永远重试损坏块）。**§11 表被双向钉住**，仅三码可重试。**`Retry-After`** 只接受 delta-seconds（拒绝 HTTP-date，离线时钟正是协议别处拒绝信任的东西），429 必须带正数延迟，限流器新增 `retryAfterSeconds` 并从**最早失败**推导而非窗口长度。**残余**：§7 的 16 个端点、HTTP 服务器/路由、`Cache-Control: no-store`、§8 块头规则、TLS 引擎、网络绑定（B02）均未做 | [T03-01](tasks/T03-01.md)、[实现证据](testing/evidence/2026-09-20/t03-01-03/summary.md) |
| 2026-09-20 | T03-01 `/v1` 线上契约层经 PR #33 合并入 `master`（合并提交 `83f7358`）；CI 四个作业在首次运行即全部通过（run 35536077474，head SHA `b013f47`）。任务**保持「进行中」**，§7 端点、HTTP 服务器、§8 块头规则、TLS 引擎与网络绑定（B02）未完成 | [PR #33](https://github.com/yanzhao77/NearSend/pull/33)、合并提交 `83f7358`、T03-01 证据 |
| 2026-09-20 | T03-01 **第四批 `t03-01-04` 完成 §7 路由表与参数校验**（Flutter 测试 565→603；协议层 204→241）。**§7 开篇规则「所有 ID 参数须先解析校验」放在匹配阶段**：路由匹配在校验之后才返回，业务逻辑**拿不到**未校验参数。19 行建模为**数据**并由测试钉住（新增/删除/改方法都会失败）。**百分号编码在整个请求目标中被拒**（`%2F` 改段数、`%2e%2e` 变穿越都不必到达匹配器）；**推论**是游标必须免转义，这是对 §7 留白格式的约束，已登记待确认。**未知方法答 `NOT_FOUND`**（§11 无 405、§3 禁止发明细节），已登记为解读。**块偏移只从索引推导**（§8），**不存在**能让客户端 offset 进来的参数。**分页上限取决于 kind**（files 128 / chunks 1024 / status 1024），§6 的「files 页不得带 fileId、chunks 页必须带」被强制。**期间由测试发现两个缺陷**：路由模板的前导空段使**每条路由都匹配失败**（15 项测试同时报错）；kind 相关上限的哨兵值落穿（`kind=files` 返回 `-1` 而非 128）。**残余**：19 个端点的实际逻辑、HTTP 服务器、鉴权决定、请求/响应体、§6 页结构与 1 MiB 字节约束、§8 块头规则、TLS 引擎、网络绑定（B02）均未做 | [T03-01](tasks/T03-01.md)、[实现证据](testing/evidence/2026-09-20/t03-01-04/summary.md) |
| 2026-09-20 | T03-01 §7 路由表与参数校验经 PR #35 合并入 `master`（合并提交 `87a0469`）；CI 四个作业在首次运行即全部通过（run 35536993196，head SHA `4fde4aa`）。任务**保持「进行中」**，19 个端点的实际逻辑、HTTP 服务器、鉴权决定、§8 块头规则、TLS 引擎与网络绑定（B02）未完成 | [PR #35](https://github.com/yanzhao77/NearSend/pull/35)、合并提交 `87a0469`、T03-01 证据 |
| 2026-09-20 | T03-01 **第五批 `t03-01-05` 完成 §6 清单分页与 seal**（Flutter 测试 603→653；协议层 241→292）。**页结构是 sealed 类型**（files 页不得带 fileId、chunks 页必须带，且 items 是不同记录），由类型本身拒绝两种被禁止的组合；页内**索引不是自由的**（第 i 项索引必须等于 `startIndex + i`）。**字节上限靠编码测量而非估算**：把候选页真的编码出来量一次，无法在 JSON 转义／十进制字段／多字节字符上出错。**诚实的发现**：按 §5.1 的 1024 字节路径上限，现实输入下**条目上限总是先起作用**，字节上限是安全网（用最大合法路径填满 128 项断言一页装得下，另用发送方不可能构造的超大条目驱动机制本身）。**重传由内容决定而非区间**（§6 拒绝的是「重叠区间**内容不一致**」）；**按索引存储**是让「不能用重复页增加累计数量」成立的结构——append 实现能通过天真的重传测试却仍会重复计数。**乱序到达仍按索引升序 seal**。**seal 按 §6 顺序检查、摘要放最后**（摘要对每种问题都失败却不说哪一种），复用 T02-01 的 `verifyDigest`（其注释当初就写了「§6 seal」）与 `chunkLengthForIndex`（对端不能靠声明短块跳过字节）。**30 分钟窗口只提供谓词**，清理动作属服务端。**残余**：19 个端点的实际逻辑、HTTP 服务器、鉴权决定、§8 块头规则、暂存持久化、窗口执行者、TLS 引擎、网络绑定（B02）均未做 | [T03-01](tasks/T03-01.md)、[实现证据](testing/evidence/2026-09-20/t03-01-05/summary.md) |
| 2026-09-20 | T03-01 §6 清单分页与 seal 经 PR #37 合并入 `master`（合并提交 `a18bf85`）；CI 四个作业在首次运行即全部通过（run 35537934004，head SHA `2a038e3`）。任务**保持「进行中」**，19 个端点的实际逻辑、HTTP 服务器、鉴权决定、§8 块头规则、暂存持久化、30 分钟窗口执行者、TLS 引擎与网络绑定（B02）未完成 | [PR #37](https://github.com/yanzhao77/NearSend/pull/37)、合并提交 `a18bf85`、T03-01 证据 |
| 2026-09-20 | T03-01 **第六批 `t03-01-06` 完成 §7 响应体契约**（Flutter 测试 653→702；协议层 292→341）。**§4 的「哪些数字是字符串」按字段落实**：`totalBytes`／`leaseEpoch`／`checkpointSeq`／块 `index` 是十进制字符串，`fileCount` 是 JSON 整数，**互换即拒绝**（把两者互换的响应，对端读不出来）；补上 `encodeDecimalString`，负数与超范围值在 §4 下**没有合法编码**。**§10 的线上状态名从枚举常量推导**而非第二张表，测试把 §10 的 16 个名字作为数据比对，`fromWireName` 对未定义名字返回 **null 而非默认值**。**凭证只出现在两种响应里**（§7）：做成 `TokenBearingResponse` **接口**，只被 `AuthorizationGrant` 与 `ResumeGranted` 实现，另有测试断言其余响应体**都不是**它；**202 形态明确拒绝令牌**（§11 说用户确认不应占用长期请求，在 202 里发令牌等于在工作完成前交出凭证）。**期间修正一处自己的不一致**：`ResumeGranted.expiresInSeconds` 原先不做正数校验而 `PairResponse` 会做，已统一并补测试。**残余**：§9 状态体与缺块区间（**形状有草案留白，故意未实现**）、§8 二进制块响应与头规则、19 个端点的实际逻辑、HTTP 服务器、鉴权决定、TLS 引擎、网络绑定（B02）均未做 | [T03-01](tasks/T03-01.md)、[实现证据](testing/evidence/2026-09-20/t03-01-06/summary.md) |
| 2026-09-20 | T03-01 §7 响应体契约经 PR #39 合并入 `master`（合并提交 `63198c9`）；CI 四个作业在首次运行即全部通过（run 35538909323，head SHA `7262bb0`）。任务**保持「进行中」**，§9 状态体、§8 二进制块响应与头规则、19 个端点的实际逻辑、HTTP 服务器、鉴权决定、TLS 引擎与网络绑定（B02）未完成 | [PR #39](https://github.com/yanzhao77/NearSend/pull/39)、合并提交 `63198c9`、T03-01 证据 |
| 2026-09-20 | T03-01 §7 响应体契约的合并记录经 PR #40 合并入 `master`（合并提交 `e6a4d22`）；CI 四个作业在首次运行即全部通过（run 35539196222，head SHA `b607c43`）。纯文档提交，任务状态未变 | [PR #40](https://github.com/yanzhao77/NearSend/pull/40)、合并提交 `e6a4d22`、T03-01 证据 |
| 2026-09-20 | T03-01 **第七批 `t03-01-07` 完成 §8 块请求/响应头与块体长度规则**（Flutter 测试 702→742；协议层 341→381）。**这些是帧定界规则，而请求走私就住在帧定界里**：一条两个跳数对「它在哪里结束」有分歧的消息，是一条可以被切成两条的消息。故做成**对头映射求值的纯函数**而非 HTTP 处理函数内部——判定与「读 body」彻底分离（**不合法的一个字节都不会被处理**），且**不需要 socket 就能测**。**「单一 `Content-Length`」在头映射里没有唯一形态**：可以是只差大小写的两个条目，也可以是逗号连接的一个值，**两种都拒绝**，因为悄悄合并就是替实现选一个长度，而选哪一个正是这条规则要防的分歧。**`Content-Length` 接受前导零而 `X-LFT-Lease-Epoch` 拒绝**：前者是 **HTTP 字段**（按 HTTP 文法是数字串），后者是**协议字段**（§4 规范十进制，一个世代只许一种拼写）——层次划分写进了文档与测试，以免被当成疏漏。**只有 `X-LFT-` 自己的命名空间拒绝未定义头**，其余无关头放行，理由同 §4 拒绝未定义 JSON 字段。**冻结清单是权威、头里的摘要是参考**：`agreesWithManifest` 如实报告不一致但**不据此拒绝**，否则等于把一个对端本来影响不了的下载失败权交给它。**块体只接受恰好等于预期长度**，短是「消息提前结束」（**可能正是跳数分歧**）、长是「声明之后又追加字节」，两者都答 `INVALID_FIELD`——§11 对该码的指示是「修正请求，**不自动原样重试**」，原样重发只会重演同一次分歧。**期间发现并修正一个真实缺陷**：`syncReceiptMismatch` 无线路码映射，使 §8 要求的 `CHUNK_HASH_MISMATCH`（422，不可重试）经兜底分支被报成 `DB_COMMIT_FAILED`（500，**可重试**），**行为上邀请客户端无限重发内容冲突的块**；两边的测试各自都是绿的（存储层只断言本地码，协议层只覆盖自己列出的码），是**把 §8 的文字与存储层实际行为对照**才发现的。**残余**：「拒绝并关闭连接」的关闭那一半需 HTTP 层；§8 的**写入顺序**与**批量 checkpoint** 未接线；§9 状态体、19 个端点的实际逻辑、HTTP 服务器、鉴权决定、TLS 引擎与网络绑定（B02）未做 | [T03-01](tasks/T03-01.md)、[实现证据](testing/evidence/2026-09-20/t03-01-07/summary.md) |
| 2026-09-20 | T03-01 §8 块请求/响应头与块体长度规则经 PR #41 合并入 `master`（合并提交 `8958e38`）；CI 四个作业在首次运行即全部通过（run 35539883079，head SHA `da54a37`）。任务**保持「进行中」**：「拒绝并关闭连接」的关闭那一半需 HTTP 层、§8 写入顺序与批量 checkpoint 未接线、§9 状态体、19 个端点的实际逻辑、HTTP 服务器、鉴权决定、TLS 引擎与网络绑定（B02）未完成 | [PR #41](https://github.com/yanzhao77/NearSend/pull/41)、合并提交 `8958e38`、T03-01 证据 |
| 2026-09-20 | T03-01 §8 块头规则的合并记录经 PR #42 合并入 `master`（合并提交 `344f99f`）；CI 四个作业在首次运行即全部通过（run 35540247223，head SHA `f66b5e6`）。纯文档提交，任务状态未变 | [PR #42](https://github.com/yanzhao77/NearSend/pull/42)、合并提交 `344f99f`、T03-01 证据 |
| 2026-09-20 | T03-01 **第八批 `t03-01-08` 完成 §8 批量 checkpoint 窗口与 `checkpointSeq`**（Flutter 测试 742→782；窗口 23 项 + 存储 14 项 + 迁移 3 项），并给出 **schema v3**。**为什么需要窗口**：§8 允许用 `verified_pending` 而不是 `committed` 回答块 `PUT`，因为字节已落盘并验证但**没有任何块行被提交**，断点没有前进——这是有意的取舍，批量提交的代价是崩溃时丢掉上次 checkpoint 之后的工作，§8 用**16 MiB** 与 **1 秒**两道界限框住它并在**暂停/文件结尾**强制提交。**窗口里有意的「没有」**：**没有第三种块状态**——`chunks.state` 仍是 `missing`/`committed`，因为一个 `verifying` 状态会成为「我们到哪了」的第二个答案（`AGENTS.md` §2 规则 5），`verified_pending` 只是**响应**，故窗口中途崩溃会把块留在 `missing` 并被重传；测试直接钉住「三个 `verified_pending` 之后已提交数为 0」。**窗口持有「收据」而非索引**：批量提交在**事务内**把每张 `(index,length,sha256)` 收据与冻结清单再比对，冲突则**整批失败、一条都不提交、序号不前进**——只记索引会让它变成一条「把任何已登记块标成已提交而无需证据」的路径；半应用的 checkpoint 会谎报进度。**「单一提交路径」被保留而非并列**：`commitChunkAfterSync` 现在**是** `CommitWindowPolicy.immediate` 下的同一个窗口路径，故标记 `state='committed'` 的 SQL 只有一处；立即策略必须用**零间隔**表达而非「容量=一个块」，否则每个文件的**尾块**（小于一个块）永不到期、永远停在 `verified_pending`，测试覆盖了这一条。**schema v3**：`tasks.checkpoint_seq INTEGER NOT NULL DEFAULT 0`——序号必须持久化，因为只活在内存里的计数器重启后从零开始、看起来正是一次回退，而 §9 让发送方验证它**不回退**；落点选 `tasks` 因为搭档 `lease_epoch` 在那里（V2.1 把它放在本仓库并不存在的 `sessions` 表，该偏离已登记）；作用域选**每任务**（§8 未点名，已登记）。`applyVersion1`/`applyVersion2` **一字未改**，并**再次自动**验证「新库与升级库的列完全一致」。**期间发现一处文档—现实漂移**：`kDbSchemaVersion = 1` 与「数据库尚未创建」在 T04-01 已实现 schema（本次之后为 3）之后**数值与措辞都不成立**，且已构成 T01-01 明令禁止的**重复定义**；因需先定 `kDatabaseImplemented` 的语义（存储层已实现 vs 应用真的会打开），**本批不擅自改**，已在 §5 登记两个候选解法。**残余**：「认证和授权→文件锁」与「等待旧写入停止」未接线、**每文件串行化**与**背压**无结构性保证、批量窗口**未做真实磁盘测量**、HTTP 层与真机均未涉及 | [T03-01](tasks/T03-01.md)、[实现证据](testing/evidence/2026-09-20/t03-01-08/summary.md) |
| 2026-09-20 | T03-01 §8 批量 checkpoint 窗口与 `checkpointSeq`（schema v3）经 PR #43 合并入 `master`（合并提交 `90e91ee`）；CI 四个作业在首次运行即全部通过（run 35541096106，head SHA `2406100`）。任务**保持「进行中」**：「认证和授权→文件锁」与「等待旧写入停止」未接线、每文件串行化与背压无结构性保证、批量窗口未做真实磁盘测量、§9 状态体、19 个端点的实际逻辑、HTTP 服务器、鉴权决定、TLS 引擎与网络绑定（B02）未完成 | [PR #43](https://github.com/yanzhao77/NearSend/pull/43)、合并提交 `90e91ee`、T03-01 证据 |

每次改变状态同时更新证据链接、适用环境、阻塞和下一动作；真实失败不得覆盖为“待验证”。历史证据不覆盖，新增运行按日期/运行ID归档。Git提交及PR提供版本追踪，不在同一提交正文猜测尚未生成的SHA。只有目标端退出门槛通过才能将平台项目从“阻塞/部分完成”改为“已完成”。
