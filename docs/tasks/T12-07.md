# T12-07 接收流程与空间预检

状态：**就绪**
依赖：T12-02、T12-03
依据：`docs/ui/UI_UX_SPEC.md` §4–§6；`lib/core/storage/space_plan.dart`

## 目标

重做接收列表、接收确认、保存位置和空间明细。

## 退出门槛

- [ ] `sufficient`、`insufficient`、`unknown` 分别表达。
- [ ] unknown 不显示绿色通过。
- [ ] known insufficient 阻止接受并给出下一步。
- [ ] 保存位置使用平台句柄/URI，不强制转换为普通路径。

## 人工重点复核

完整空间峰值、未知空间风险确认、已导出文件不被清理删除。
