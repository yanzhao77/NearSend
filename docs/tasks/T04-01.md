# T04-01 SQLite schema、迁移框架与 chunk repository

- 基线分支/SHA：`master` @ `c38a5e17647c51a08c5f8e6760d01a7a0b989cc1`
- 负责人或 Agent：开发 Agent（DeepSeek Harness）
- 状态：进行中（schema v3、存储层、统一状态 codec 与 v1→v2→v3 迁移已实现并测试；manifest staging 尚未落 SQLite，真实暂存损坏、OS 层满盘、真机 `syncData` 未完成）
- 依赖：T01-01（已完成）、T02-01/T02-02（已完成）；B04 的真机故障注入可后补
- 分支 / PR：`feat/t04-01-sqlite-repository`、`feat/t04-01-storage-core`、`feat/t04-01-idempotency`、`feat/t04-01-transfer-peers`、`feat/t04-01-disk-full-injection` → [#13](https://github.com/yanzhao77/NearSend/pull/13) / [#15](https://github.com/yanzhao77/NearSend/pull/15) / [#17](https://github.com/yanzhao77/NearSend/pull/17) / [#19](https://github.com/yanzhao77/NearSend/pull/19)
- 关联规格：
  - `AGENTS.md` §2 核心规则 5/6/7、§5 安全规则、§7 必做检查
  - `docs/protocol/v1.0-draft1.md` §8（写入与持久化确认顺序）、§9（幂等与世代）
  - `docs/architecture/SYSTEM_ARCHITECTURE.md` §8（持久化模型）、§9（并发与资源）
  - `docs/architecture/APP_AND_SERVICE_DESIGN.md` §7（接收端六步提交）、§10（数据库与迁移）
  - `docs/testing/QUALITY_AND_ACCEPTANCE.md` §3（存储与恢复必测矩阵）
  - [ADR-0003 SQLite 绑定](../decisions/ADR-0003-SQLite绑定与耐久性配置.md)
  - [ADR-0004 staging 持久化与恢复权威](../decisions/ADR-0004-staging持久化与恢复权威.md)
- 风险级别：存储

## 目标

建立接收方的持久化层：schema 与迁移框架、committed 块的唯一权威记录、durable checkpoint 事务、
恢复租约的获取与失效，以及高版本 schema 拒绝写入。使「断点续传只信 committed 块」这条核心规则
有一个可测试的实现，而不是靠调用方自觉。

## 范围内

- **schema 与版本**：`schema_version` 单一权威；表 `tasks`、`files`、`chunks`、`peers`、
  `idempotency`、`exports`，字段取自 `SYSTEM_ARCHITECTURE.md` §8。
- **迁移框架**：单调递增版本、逐版本迁移函数、升级前一致性备份（使用 SQLite backup API，
  而非复制活跃 WAL 的主文件）、迁移失败回滚并保留旧数据。
- **高版本拒绝写入**：数据库 schema 版本高于本构建时**拒绝打开写入**，不尝试修改。
- **chunk repository**：
  - `(file_id, index)` 唯一；`chunks.state` 区分未提交与 committed。
  - `commitChunkAfterSync`：**只有**在调用方已确认 durable sync 之后才允许提交块；
    repository 不替调用方决定顺序，但接口形状要让错误顺序难以表达。
  - committed 块是恢复的唯一权威；提供「列出缺失块」的查询，且该查询不得读取任何进度字节数。
- **恢复租约**：`acquireResumeLease` 提升 `lease_epoch`；`assertLease` 在每次写入前校验；
  旧世代写入被拒绝。复用 T02-02 的 `LeaseGuard` 语义，不重复实现规则。
- **幂等记录持久化**：与效果在**同一事务**中提交，使记录与效果不会分叉；
  复用 T02-02 的 `IdempotencyStore` 规则。
- **故障注入测试**（见「必做测试」）。

## 范围外

- 平台的 `syncData` 真实语义与断电耐久性结论（**B04 真机验证**；本任务只保证调用顺序与可测试的替身）。
- 空间计算、导出与清理（T06-01）。
- 网络传输、配对、HTTP 路由（T03-01）。
- UI 与任务编排（T03-02/T05）。
- 后台 isolate 的具体划分（`SYSTEM_ARCHITECTURE.md` §12 仍未冻结；本任务提供可在 isolate 中调用的接口，
  但不在本任务内决定线程模型）。

## 接口与不变量

- **committed 块是恢复的唯一权威。** `receivedBytes`/`writtenBytes`/UI 进度**不得**参与恢复决策；
  repository 不提供这类字段的读取接口用于恢复。
- 提交顺序由调用方保证为 `写入 → 块哈希校验 → durable sync → 事务提交 → 确认`；
  接口必须使「未 sync 就 commit」在类型或前置校验上难以发生。
- 同一文件同一时刻只有一个写入者；写入前校验 `lease_epoch`。
- 迁移失败必须回滚且旧数据可读；**不得**留下半迁移状态。
- 高于本构建的 schema 版本 → 拒绝写入，且不得修改数据库。
- 恢复凭证、密钥、令牌**不得**写入普通 SQLite 字段（`AGENTS.md` §5）；只存验证摘要或引用。
- 协议/领域状态与 SQLite 状态只通过 `StorageStateCodec` 转换；业务代码不得手写数据库状态字符串。
- 断点恢复只信接收端 SQLite 已持久化的 committed 块与 checkpoint；发送端记录仅为镜像。

## 验收条件

- [x] schema 版本单调递增；迁移运行器与回滚测试通过；当前 schema v3，端到端覆盖新建库、1→2 与 2→3，并断言新库与升级库列一致。
- [x] 迁移中途失败时回滚，旧数据仍可读，且数据库未被标记为已迁移。
- [x] 高版本 schema 被拒绝写入，且未发生任何写入（以 `PRAGMA journal_mode` 未被改为 WAL 为证）。
- [x] `commitChunkAfterSync` 在未确认 durable sync 时拒绝提交（提交只能经由接收 `DurableChunkSink` 的这条路径）。
- [x] committed 块权威：committed 块退回 `missing` 后，缺失块查询能正确识别。
- [x] 旧 `lease_epoch` 写入被拒绝；恢复提升世代后旧写入不再落盘（含「写入期间发生恢复」的窗口）。
- [x] 幂等记录与效果同事务提交；重复请求返回同一结果。
- [x] `tasks`/`files` 状态迁移只走 T02-02 状态机的已定义边；未定义的边被拒绝且不写入任何内容。
- [x] `tasks`/`files` 的状态编码已收口到 `StorageStateCodec`，线上名与数据库名不在业务代码中混用。
- [ ] manifest staging 页、首次内容时间与 seal 结果持久化到 SQLite，并通过未 seal/已 seal 重启恢复测试。
- [x] 导出记录仅在文件全部 committed 后写入；同一文件不静默产生第二份副本；失败记录不覆盖已保存记录。
- [x] peer 指纹变化只报告不吸收，只有用户显式授权才改变已存信任；`peers` 表无令牌/密钥列。
- [x] 空间耗尽被报告为 `SPACE_INSUFFICIENT`（507，**不可重试**），不与可重试的 `DB_COMMIT_FAILED` 混淆；卷满不丢已提交断点。
- [ ] 故障窗口测试全部通过。**部分完成**：磁盘满已由引擎真实注入（`SQLITE_FULL`，见 `t04-01-05`），
  但真实暂存损坏/丢失、OS 层 `ENOSPC` 与断电仍未覆盖，见下。
- [x] `dart format`、`flutter analyze`、`flutter test` 通过；CI 四个作业通过。

## 本次结果

### `t04-01-02`：schema、迁移与 chunk repository

已实现 `storage_schema.dart`、`storage_failure.dart`、`storage_migrations.dart`、
`near_send_database.dart`、`chunk_repository.dart` 与 38 项测试（Flutter 测试总数 168 → 201）。
详见[运行汇总](../testing/evidence/2026-09-20/t04-01-02/summary.md)。

**期间修复**：`writeBackup` 原先在 `try` 之外创建备份目录，目标不可写时抛出的是原始
`PathExistsException` 而不是 `StorageException(backupFailed)` —— 调用方唯一需要处理的失败条件
却拿到未类型化异常。已修复并由测试固定。

### `t04-01-03`：幂等持久化

已实现 `idempotency_repository.dart` 与 13 项测试（Flutter 测试总数 201 → 214）。
规则复用 T02-02 的定义，本文件只负责持久化。核心保证是**记录与效果在同一事务提交**，
并以「效果写入标记行 + 断言标记行与记录同时存在/同时回滚」直接验证，而不是只断言返回值。
详见[运行汇总](../testing/evidence/2026-09-20/t04-01-03/summary.md)。

### `t04-01-04`：任务/文件状态、导出记录与 peer 授权

已实现 `transfer_repository.dart`、`peer_repository.dart` 与 22 项测试
（Flutter 测试总数 214 → 236；存储测试 51 → 73）。
详见[运行汇总](../testing/evidence/2026-09-20/t04-01-04/summary.md)。

三条不变量由代码而非调用方保证：状态迁移只能在 T02-02 状态机的已定义边上发生；
导出只在文件全部 committed 后才能记录，且同一文件不会静默产生第二份副本；
peer 指纹变化只**报告**不吸收，只有用户的显式授权才改变已存信任。

**期间修复**：`NearSendDatabase.transaction` 把非 `StorageException` 的失败一律包装成
`NS-STORAGE-*`，导致状态机抛出的 `ProtocolViolation`（未定义的状态迁移）到达调用方时
被报告为「磁盘失败」。按错误码做重试决策的调用方会据此误判。已改为原样重抛
`ProtocolViolation`；该缺陷由本次新增测试发现。

### `t04-01-05`：磁盘满与提交失败注入

已实现 8 项注入测试（Flutter 测试总数 236 → 244；存储测试 73 → 81），并修复一处真实缺陷。
详见[运行汇总](../testing/evidence/2026-09-20/t04-01-05/summary.md)。

**注入方式先经探针实测**：`PRAGMA max_page_count` 钉住页数后继续写入，引擎真实返回
result code 13（`SQLITE_FULL`），因此磁盘满条件由**引擎产生而非模拟**。探针同时给出一个
**否定结论**：块提交是单行 `UPDATE`，即使数据库完全写不进去也**成功**，因为它不分配新页。
所以注入位置必须在 sink（真实产品里写满磁盘的是块数据），数据库侧条件另经事务原语验证。

**期间修复**：真实的 `SQLITE_FULL` 原先经 `transaction` 被包装为
`NS-STORAGE-006 commitFailed` → `DB_COMMIT_FAILED`（500，**可重试**）。
对写不进去的卷自动重试不会成功，且 §11 为 `SPACE_INSUFFICIENT` 规定的唯一有效动作
（清理空间或更换位置）永远不会呈现给用户。已新增 `NS-STORAGE-008 spaceInsufficient`
→ `SPACE_INSUFFICIENT`（507，**不可重试**），并在抛出点按主结果码区分。

### `t04-01-06`：多版本迁移（由 T06-01 的 schema v2 解锁）

本卡第 3 项剩余「目前只有 schema v1；多版本迁移待 v2 出现后端到端验证」在
`t06-01-04` 中解除：T06-01 为记住导出的落盘名字给 `exports` 增加了 `saved_path`，
`currentVersion` 升到 **2**，迁移框架第一次有了真实的升级路径。
`storage_migration_test.dart` 新增 7 项测试，其中 6 项覆盖 **1→2 端到端**：
达到当前版本、旧行保留、旧导出行的名字是 `NULL` 而不是编造的冻结路径、
升级前写了一致性备份且备份可读、升级后可以写入名字、
**新库与升级库的列完全一致**（后一条是防止「改 v1 建表语句」这类漂移的关键断言）。

**`applyVersion1` 一个字都没改**：现场数据库已经跑过它，改它会让新库和升级库产生
没有任何测试会察觉的差异。v2 用 `ALTER TABLE ... ADD COLUMN`（SQLite 中事务性且保留每一行）。

**期间发现**：升级测试最初想把注册表截断到 v1 来造库，被 `assertWellFormed` **正确拒绝**
（它要求注册表恰好达到 `currentVersion`，这对出厂注册表是正确的守卫）。
测试改为直接调用 `applyVersion1` 重建 v1 构建当时的状态，
**没有为了让测试通过而放宽守卫**。
详见 [T06-01 运行汇总](../testing/evidence/2026-09-20/t06-01-04/summary.md)。

## 仍剩余（任务保持「进行中」）

1. 真实暂存文件损坏/丢失（依赖 T06-01 的暂存层）。
2. 平台 `syncData` 真机语义（B04）——真机断电耐久性仍未证实。
3. 批量 checkpoint 窗口与背压未测量。
4. 真实满盘文件系统（OS 返回 `ENOSPC`、部分写入的块文件、WAL 写满）未注入；
   `t04-01-05` 的卷满由 SQLite 引擎产生，**不等价**于操作系统层的满盘。
5. 升级路径已在 **1→2** 与 **2→3** 上验证；未来出现 v4 时必须重复同样的验证。


## 必做测试

覆盖 `QUALITY_AND_ACCEPTANCE.md` §3「存储与恢复」矩阵：

| 场景 | 期望 |
| --- | --- |
| 块写入前崩溃 | 无 committed 记录，缺失块查询包含该块 |
| 写入中崩溃（部分字节） | 同上；不得出现长度不符的 committed |
| durable sync 前崩溃 | 不得提交；块仍为缺失 |
| sync 后、DB 提交前崩溃 | 不得出现 committed；重传后一致 |
| DB 提交后崩溃 | 块为 committed，恢复不需重传 |
| 磁盘满 | 提交失败且不产生 committed；保留可恢复状态 |
| DB 提交失败 | 不确认该块；状态可重试 |
| 暂存文件损坏 | 恢复/终检识别为损坏块，退回缺失 |
| 暂存文件丢失 | 同上，不得静默跳过 |
| 旧 lease 写入 | 拒绝 |
| 重复请求（同 request_id） | 返回同一结果，不重复写入 |
| 重新打开数据库（模拟重启） | 断点与 committed 块保持一致，不依赖内存状态 |

**本任务使用进程内故障注入与真实 SQLite 文件**；**断电**、真实磁盘满与平台 `syncData` 结论属 B04，
不得用本任务的结果代替。

## 交付物

- `lib/core/storage/`：schema、迁移、repository、租约与幂等持久化
- `test/core/storage/`：迁移、故障注入、权威性与幂等测试
- 证据：`docs/testing/evidence/2026-09-20/t04-01-01/` … `t04-01-04/`

## 台账回填

- `docs/PROJECT_LEDGER.md`：§2（新增存储成果行）、§4（T04）、§4.1（T04-01 状态）、§5、§7。

## 备注：本任务开始前已完成的两项前置

1. **绑定决策与验证**（[ADR-0003](../decisions/ADR-0003-SQLite绑定与耐久性配置.md)）：
   选用 `sqlite3`，否决 `sqflite`/`drift`/自建绑定，并**移除已废弃且为空的 `sqlite3_flutter_libs`**。
   探针 `test/core/storage/sqlite_binding_probe_test.dart` 在 Windows 上 5/5 通过，
   运行时库 SQLite 3.53.4，`synchronous=FULL` 读回为 2。
2. **范围定案**：本卡把「待澄清」具体化为上述范围内/外与验收条件。
