# T03-01 同网二维码配对与信任

- 基线分支/SHA：`master` @ `2f9bdca5a34d1e100b488e6f127d86c5e9bb439e`
- 负责人或 Agent：开发 Agent（DeepSeek Harness）
- 状态：进行中（配对载荷、信任上下文、令牌生命周期、`/v1/pair` 模型与 `/v1` 线上契约层已实现并测试；
  目标端网络绑定仍**阻塞于 B02**；§7 的 16 个端点、HTTP 服务器与 TLS 引擎未实现）
- 依赖：T01-01（已完成）、T02-02（已完成）、**B02（阻塞中，仅影响网络绑定部分）**
- 分支：`feat/t03-01-pairing-trust`
- 关联规格：
  - `AGENTS.md` §2 核心规则 8（先验证指纹再提交令牌；禁止全局信任所有证书、明文降级或自制加密协议）、§5 安全规则、§7 必做检查（安全变更验证）
  - `docs/protocol/v1.0-draft1.md` §2（传输层和身份）、§3（QR 与会话）、§4（JSON、整数与统一约束）、§11（错误码）
  - `docs/architecture/SYSTEM_ARCHITECTURE.md` §10（安全边界）
  - `docs/architecture/APP_AND_SERVICE_DESIGN.md` §6（网络与发现）、§9（平台端口）
  - `docs/testing/QUALITY_AND_ACCEPTANCE.md` §3（安全必测矩阵）、§9（发布阻断项）
  - `docs/PROJECT_LEDGER.md` §3 B01/B02、§5
- 风险级别：安全

## 目标

让「用户扫码 → 确认对端身份 → 才交出一把只能消费一次的令牌」这条链路有一个
**不依赖调用方纪律**的实现：令牌在指纹比对成功之前**取不到**，而不是「文档说不要提前发」。

同时把 QR 载荷的解析做成严格解析：它是客户端从不受信来源读到的第一个东西，
并且决定「连到哪里」和「信任谁」。

## 范围内

- **配对载荷（§3/§4）**：`kind`、协议版本、`serverFingerprint`、`sessionId`、
  `candidates`（≤8，`host` 必须是裸地址、`port` 1–65535）、`pairToken`（base64url 无填充 32 字节）、
  `expiresInSeconds`；UTF-8 ≤ 4096 字节；**未定义字段一律拒绝**（§4）。
- **base64url 规范编码（§4）**：无填充、URL 字母表、**解码后重新编码必须相同**。
- **指纹计算（§2）**：`SHA-256(叶证书完整 DER)` 小写 64 hex。
  **不是** PEM 文本摘要，**不是** SPKI 摘要。
- **每连接信任上下文（§2）**：比对成功前不得释放令牌；比对失败关闭连接且不得复用；
  未使用前 `abandon()` 可关闭。
- **安全负例**：错误指纹、大写指纹、非规范 base64url、带 scheme/凭证/路径/查询的 host、
  保留字段、超限载荷等。

## 范围外

- **目标端网络绑定与候选地址可达性（B02 阻塞）**：指定 Network、多网卡选择、
  地址是否真的在本地链路——**本任务不做也不声称**。
- TLS 引擎的选择与平台集成（`SYSTEM_ARCHITECTURE.md` §12 未冻结）。
- `POST /v1/pair` 的 HTTP 传输、`sessionAccessToken` 的签发与交付、恢复凭证（属 T03-01 后续批次）。
- 配对令牌的服务端生命周期（单次消费、300 秒、重启失效、限流）——属后续批次。
- 二维码的生成与相机扫描 UI（属 T05）。

## 接口与不变量

- **令牌在比对成功之前取不到。** 这不是文档约定：`PairingHandshake` 只通过一个 getter
  暴露令牌，且该 getter 在未比对或已关闭时抛 `PAIR_REJECTED`。调用方没有「顺序」需要记住。
- **比对失败即关闭连接**（§2），且**不得在同一上下文上重新协商**——
  在对端身份校验已失败的连接上重试，正是重试循环变成攻击的方式。
- **每连接隔离**：一个连接的失败不泄漏到另一个连接。
- **不够用的证书编码也关闭连接**：不变量是「除非比对显式返回 trusted，否则关闭」。
- **指纹按 DER 计算**：参数类型是字节而非字符串，因此没有「传 PEM」的重载。
- **比较不是常数时间**：指纹发布在用户扫描的二维码里，不是秘密，时序信道不泄漏任何
  攻击者尚未拥有的东西；手写常数时间比较等于为不存在的问题发明密码学代码（`AGENTS.md` §5）。
- **候选地址选择不得绕过 pin**（§3）：本任务只做词法校验，可达性判定属 B02。

## 验收条件

- [x] QR 载荷严格解析：必填字段、类型、范围、UTF-8 上限；未定义字段被拒绝。
- [x] `serverFingerprint` 必须是 64 位**小写**十六进制；大写、长度不符、非 hex、base64 形状均被拒绝。
- [x] `pairToken` 必须是 32 字节的**规范**无填充 base64url；带填充、长度不符、非规范写法、标准字母表均被拒绝。
- [x] `candidates`：空列表、超过 8 个、端口 0/65536、端口为字符串被拒绝。
- [x] `host` 带 scheme / 凭证 / 路径 / 查询 / 空白 / 括号被拒绝；IPv4 越界与前置零被拒绝；主机名被拒绝。
- [x] 指纹是 SHA-256(DER)，且**有测试证明它不等于 PEM 文本摘要**、也不等于对 DER 一部分的摘要。
- [x] 比对成功前读取令牌抛 `PAIR_REJECTED`；成功后可用。
- [x] 比对失败关闭上下文，令牌**永久**不可用，且同一上下文不得重新协商。
- [x] 一个连接的失败不影响另一个连接（每连接隔离）。
- [x] 配对令牌：300 秒窗口以**单调计时**判定；单次消费；服务重启后未消费令牌全部失效；响应丢失不恢复令牌。
- [x] 失败对端**统一** `PAIR_REJECTED`，本地理由不可从线上答案到达；限流返回 429。
- [x] 限流：每来源 5 次/分钟与全局 30 次/分钟两个滑动窗口，来源间互不影响，失败随时间移出窗口。
- [x] `POST /v1/pair` 请求与响应模型严格解析；`clientLabel` 按 **UTF-8 字节**上限 128 校验。
- [x] capability 词表**未被发明**：复用 T02-02 的 `CapabilitySet`，空列表为合法答案。
- [x] 控制体严格解码：≤1 MiB、拒绝 BOM、拒绝非法 UTF-8、**拒绝重复对象键**（含转义拼写）、深度 ≤16。
- [x] 线上错误体 `{code,message,retryable,requestId?}`：`message` 由错误码的 `messageKey` 生成，调用方**无法**放入自由文本；`retryable` 必须与 §11 一致。
- [x] §11 的状态表被测试**双向**钉住；只有三个码标记为可重试。
- [x] `Authorization: Bearer` 严格解析；**不存在**从查询串读令牌的解析器。
- [x] `Retry-After` 只接受 delta-seconds（拒绝 HTTP-date）；429 必须带正数延迟；限流器据最早失败给出等待秒数。
- [x] `dart format`、`flutter analyze`、`flutter test` 通过；CI 四个作业通过。
- [ ] 目标端网络绑定与候选地址可达性（**阻塞于 B02**）。
- [ ] §7 的 16 个端点、HTTP 服务器/路由、`Cache-Control: no-store`、§8 的块头规则；会话访问令牌的签发与交付。

## 本次结果

### `t03-01-01`：配对载荷与「pin 先于令牌」的信任上下文

已实现 `pairing_payload.dart`、`pairing_trust.dart`、`base64url.dart` 与 68 项测试
（Flutter 测试总数 358 → 426）。
详见[运行汇总](../testing/evidence/2026-09-20/t03-01-01/summary.md)。

`lib/core/security/` 目录由此建立（`AGENTS.md` §4 目录规则要求该层存在）。

**最重要的一条不变量做成结构性而非约定性**：§2 要求「必须在发送令牌、HTTP 请求及文件内容之前
完成指纹比对」。这种规则很容易写下来、也很容易写错，因为**写错的实现仍然能与诚实服务端正常工作**。
所以令牌不是一个字段，而是只能经 `PairingHandshake.pairingToken` 取得，
该 getter 在比对成功前抛 `PAIR_REJECTED`——调用方没有顺序需要记住。

**指纹的三种「形状相同但语义不同」的写法**（DER / PEM 文本 / SPKI）都有测试固定差异，
因为弄错会得到一个看似合理却永远匹配不上的 pin。

### `t03-01-02`：配对令牌生命周期与 `/v1/pair` 模型

已实现 `pairing_token.dart`、`pair_request.dart` 与 60 项测试
（Flutter 测试总数 426 → 486；安全层测试 68 → 128）。
详见[运行汇总](../testing/evidence/2026-09-20/t03-01-02/summary.md)。

**令牌生命周期**：300 秒窗口以**单调计时**判定（墙上时钟可被 NTP 或用户改时间移动，
那会延长令牌寿命或让活令牌立刻过期）；单次消费；**服务重启失效是构造性的**——
不持久化任何令牌状态，所以没有「可能被漏掉的失效步骤」；响应丢失后没有任何 API 能恢复令牌。

**只保存摘要**：§2 已为恢复密钥确立「服务端仅保存验证摘要」，配对令牌同样处理。
**诚实标注**：这一条**没有测试覆盖**（Flutter 无 `dart:mirrors`，无法断言内部字段），
由代码结构保证，已在运行汇总里明确写成「未经测试」而不是含糊带过。

**比较不声称常数时间，并说明原因**：Dart 不提供时序保证（JIT 可重排），
手写「常数时间」循环会是一种**虚假保证**。真正约束风险的是 256 位熵、单次消费、
300 秒窗口、滑动限流与「只比较摘要」。

**统一失败做成结构性**：`PairingRejection` 只用于本地诊断，
而 `wireCode` 对三种拒绝原因是**同一个常量**，测试断言这一点，并断言本地理由从线上答案不可达。

**限流在令牌之前检查**：已被限流的调用方即使拿的是**正确**令牌也只得 `RATE_LIMITED`，
因此无法靠「换令牌再试」探测哪个令牌存在（有测试覆盖）。

**capability 词表不发明**：复用 T02-02 的 `CapabilitySet`，测试全程用合成标识符。

**期间发现一个协议内在张力（已登记，未修）**：§9 要求 `requestId` 幂等重放，
但 §3 要求配对响应丢失后**不恢复**已消费令牌——配对是 §9 幂等的一个例外。
本层只校验 `requestId` 格式，**没有**实现任何幂等重放语义，因为对配对而言重放正是 §3 禁止的事。

### `t03-01-03`：`/v1` 线上契约层

已实现 `json_body.dart`、`wire_error.dart` 与 79 项测试
（Flutter 测试总数 486 → 565；协议层测试 124 → 204）。
详见[运行汇总](../testing/evidence/2026-09-20/t03-01-03/summary.md)。

**控制体不能只交给 `jsonDecode`**：Dart 的解码器**不拒绝重复的对象键**（保留最后一个、什么也不说），
这正是解析器差分的形状——两个实现读同一批字节、得出不同的值。
所以先扫描一次结构（深度、重复键、转义）再取值；扫描器**刻意不构建值**，
因为两个都构建的解析器就是两次产生分歧的机会。

**重复键可以藏在转义里**：`{"a":1,"\u0061":2}` 是同一个键的两种拼写。
比较原始文本会判为合法，所以扫描器**先解码转义再比较**（含代理对）。
另外，重复键的**报错不回显键名**——键来自请求体，而控制体可以携带令牌。

**错误体的 `message` 不是自由文本**：`WireError.of(code)` 不接受 message 参数，
只写入该码的 `messageKey`（封闭常量集），所以调用方传什么都到不了这个字段。
这把 T02-02 那句注释（"A key rather than a string: §7 requires the message to carry no secret"）
变成了结构。**收到的** message 不同：对端可发任意文本，只限长并查控制字符，
**不**猜测它是否含密钥（那种启发式只会带来虚假的安心）。

**`retryable` 是校验而非采信**：一个发来 `retryable: true` + `CHUNK_HASH_MISMATCH` 的对端
等于邀请客户端**永远重试一个损坏的块**，所以解析值必须与 §11 的行为一致，两个方向都测。

**§11 的表被双向钉住**：表中每个码的状态必须一致，且枚举里不能有表中未出现的码。

**`Retry-After`**：只接受 delta-seconds，**拒绝 HTTP-date**（离线时钟正是协议别处拒绝信任的东西）；
429 必须带正数延迟；限流器新增 `retryAfterSeconds`，从**最早的失败**何时移出窗口推导，
而不是窗口长度——在 20 秒就够的时候让客户端等满一分钟是另一种错。

## 仍剩余（任务保持「进行中」）

1. **目标端网络绑定与候选地址可达性（阻塞于 B02）**：指定 Network、多网卡、地址是否真的在本地链路。
   本任务只做词法校验，**不声称**任何可达性结论。
2. **§7 的 16 个端点一个都没实现**：本次只做了共享契约层（请求体解码、错误体、认证头、退避头）。
   `POST /pair`、`GET /offers`、`POST /transfers`、清单分页、块 PUT/GET、`/checkpoint`、
   `/pause`、`/complete`、`/cancel`、`/control` 等全部未实现，请求/响应字段也未建模。
3. **没有 HTTP 服务器或客户端**：没有路由、没有 `Cache-Control: no-store`（§7 要求所有控制响应带它）、
   没有 `Content-Type` 写出；`WireError` 会生成与解析但**没有任何地方发出它**。
4. **§8 的块头规则未实现**：单一 `Content-Length`、不支持 `Transfer-Encoding`、不许压缩、
   `Content-Length` 与 `Transfer-Encoding` 同时出现必须拒绝、末尾多余数据拒绝并关闭连接。
5. **会话访问令牌的签发与交付**：`sessionAccessToken` 目前由调用方提供；
   恢复凭证与 receipt 交付（§3）未实现。
6. **TLS 引擎集成**：真正取得叶证书 DER 并驱动 `PairingHandshake`；
   离线 pin 模式下的证书日期与主机名策略（§2 要求由目标引擎原型明确）。
7. **二维码生成与扫描 UI**（T05），以及相机 / 本地网络权限路径。
8. 限流状态目前仅在进程内，重启清零——§3 未要求持久化，但真机实现时需确认该读法。

## 必做测试

覆盖 `QUALITY_AND_ACCEPTANCE.md` §3「安全」矩阵：

| 场景 | 期望 |
| --- | --- |
| 正确的指纹 | 比对成功，令牌可用 |
| 错误/变化的指纹 | 关闭连接，令牌永久不可用 |
| 差一个字节的证书 | 视为不匹配 |
| 未比对就取令牌 | 抛 `PAIR_REJECTED` |
| 比对失败后再次比对 | 抛 `PAIR_REJECTED`，不得重新协商 |
| 一个连接失败 | 另一连接不受影响 |
| 指纹写成 PEM 摘要 | 有测试证明与协议指纹不同 |
| 大写指纹 / 非 hex / 长度不符 | 拒绝 |
| 令牌带填充 / 非规范写法 / 标准字母表 | 拒绝 |
| host 带 scheme、凭证、路径、查询 | 拒绝 |
| 未定义字段 | 拒绝，不静默丢弃 |
| 令牌在窗口内 / 到点 | 接受 / 拒绝 |
| 同一令牌第二次消费 | 拒绝，且与未知会话、错误令牌在线上不可区分 |
| 服务重启后消费旧令牌 | 拒绝（未持久化任何令牌状态） |
| 失败达每来源或全局上限 | 429 `RATE_LIMITED`；正确令牌同样被拒 |
| 失败移出窗口 | 不再限流 |
| `clientLabel` 64 个双字节字符 / 65 个 | 接受 / 拒绝（按字节而非字符计量） |
| capability 列表含重复或空格 | 拒绝；空列表接受（词表未定义） |
| 控制体带 BOM / 非法 UTF-8 / 超 1 MiB | 拒绝（超限为 413 `BODY_TOO_LARGE`） |
| 控制体含重复对象键（含 `\u0061` 拼写） | 拒绝，且报错不回显键名 |
| 控制体深度超过 16 | 拒绝 |
| 错误体 `retryable` 与 §11 不符 | 拒绝（两个方向） |
| 错误体含未知字段或未知 code | 拒绝 |
| §11 表 | 表中每个码的状态一致；枚举无表外码；仅三个码可重试 |
| `Authorization` 缺 scheme / 空令牌 / 多余空白 | 拒绝 |
| `Retry-After` 为 HTTP-date 或缺失 | 拒绝（429 必须带正数秒） |
| 按 `retryAfterSeconds` 等待后 | 限流确实解除 |

**本任务使用纯 Dart**；真实 TLS 引擎、真实网络绑定与真实设备上的可达性属 B02/B03 真机验证，
不得用本任务结果代替。

## 交付物

- `lib/core/security/`：配对载荷、信任上下文
- `lib/core/protocol/`：base64url 规范编码、配对相关协议限额
- `test/core/security/`：载荷与信任测试
- 证据：`docs/testing/evidence/2026-09-20/t03-01-01/` … `t03-01-03/`

## 台账回填

- `docs/PROJECT_LEDGER.md`：§4（T03 行）、§4.1（T03-01 状态）、§5、§7。
