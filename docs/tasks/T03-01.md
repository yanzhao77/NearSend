# T03-01 同网二维码配对与信任

- 基线分支/SHA：`master` @ `2f9bdca5a34d1e100b488e6f127d86c5e9bb439e`
- 负责人或 Agent：开发 Agent（DeepSeek Harness）
- 状态：进行中（配对载荷、信任上下文、令牌生命周期、`/v1/pair` 模型、`/v1` 线上契约层、
  §7 路由表、§6 清单分页/seal 与 §7 响应体已实现并测试；目标端网络绑定仍**阻塞于 B02**；
  §9 状态体、HTTP 服务器与 TLS 引擎未实现）
- 依赖：T01-01（已完成）、T02-02（已完成）、**B02（阻塞中，仅影响网络绑定部分）**
- 分支：`feat/t03-01-pairing-trust`
- 关联规格：
  - `AGENTS.md` §2 核心规则 8（先验证指纹再提交令牌；禁止全局信任所有证书、明文降级或自制加密协议）、§5 安全规则、§7 必做检查（安全变更验证）
  - `docs/protocol/v1.0-draft1.md` §2（传输层和身份）、§3（QR 与会话）、§4（JSON、整数与统一约束）、§11（错误码）
  - `docs/architecture/SYSTEM_ARCHITECTURE.md` §10（安全边界）
  - `docs/architecture/APP_AND_SERVICE_DESIGN.md` §6（网络与发现）、§9（平台端口）
  - `docs/testing/QUALITY_AND_ACCEPTANCE.md` §3（安全必测矩阵）、§9（发布阻断项）
  - `docs/PROJECT_LEDGER.md` §3 B01/B02、§5
- 风险级别：安全

## 目标

让「用户扫码 → 确认对端身份 → 才交出一把只能消费一次的令牌」这条链路有一个
**不依赖调用方纪律**的实现：令牌在指纹比对成功之前**取不到**，而不是「文档说不要提前发」。

同时把 QR 载荷的解析做成严格解析：它是客户端从不受信来源读到的第一个东西，
并且决定「连到哪里」和「信任谁」。

## 范围内

- **配对载荷（§3/§4）**：`kind`、协议版本、`serverFingerprint`、`sessionId`、
  `candidates`（≤8，`host` 必须是裸地址、`port` 1–65535）、`pairToken`（base64url 无填充 32 字节）、
  `expiresInSeconds`；UTF-8 ≤ 4096 字节；**未定义字段一律拒绝**（§4）。
- **base64url 规范编码（§4）**：无填充、URL 字母表、**解码后重新编码必须相同**。
- **指纹计算（§2）**：`SHA-256(叶证书完整 DER)` 小写 64 hex。
  **不是** PEM 文本摘要，**不是** SPKI 摘要。
- **每连接信任上下文（§2）**：比对成功前不得释放令牌；比对失败关闭连接且不得复用；
  未使用前 `abandon()` 可关闭。
- **安全负例**：错误指纹、大写指纹、非规范 base64url、带 scheme/凭证/路径/查询的 host、
  保留字段、超限载荷等。

## 范围外

- **目标端网络绑定与候选地址可达性（B02 阻塞）**：指定 Network、多网卡选择、
  地址是否真的在本地链路——**本任务不做也不声称**。
- TLS 引擎的选择与平台集成（`SYSTEM_ARCHITECTURE.md` §12 未冻结）。
- `POST /v1/pair` 的 HTTP 传输、`sessionAccessToken` 的签发与交付、恢复凭证（属 T03-01 后续批次）。
- 配对令牌的服务端生命周期（单次消费、300 秒、重启失效、限流）——属后续批次。
- 二维码的生成与相机扫描 UI（属 T05）。

## 接口与不变量

- **令牌在比对成功之前取不到。** 这不是文档约定：`PairingHandshake` 只通过一个 getter
  暴露令牌，且该 getter 在未比对或已关闭时抛 `PAIR_REJECTED`。调用方没有「顺序」需要记住。
- **比对失败即关闭连接**（§2），且**不得在同一上下文上重新协商**——
  在对端身份校验已失败的连接上重试，正是重试循环变成攻击的方式。
- **每连接隔离**：一个连接的失败不泄漏到另一个连接。
- **不够用的证书编码也关闭连接**：不变量是「除非比对显式返回 trusted，否则关闭」。
- **指纹按 DER 计算**：参数类型是字节而非字符串，因此没有「传 PEM」的重载。
- **比较不是常数时间**：指纹发布在用户扫描的二维码里，不是秘密，时序信道不泄漏任何
  攻击者尚未拥有的东西；手写常数时间比较等于为不存在的问题发明密码学代码（`AGENTS.md` §5）。
- **候选地址选择不得绕过 pin**（§3）：本任务只做词法校验，可达性判定属 B02。

## 验收条件

- [x] QR 载荷严格解析：必填字段、类型、范围、UTF-8 上限；未定义字段被拒绝。
- [x] `serverFingerprint` 必须是 64 位**小写**十六进制；大写、长度不符、非 hex、base64 形状均被拒绝。
- [x] `pairToken` 必须是 32 字节的**规范**无填充 base64url；带填充、长度不符、非规范写法、标准字母表均被拒绝。
- [x] `candidates`：空列表、超过 8 个、端口 0/65536、端口为字符串被拒绝。
- [x] `host` 带 scheme / 凭证 / 路径 / 查询 / 空白 / 括号被拒绝；IPv4 越界与前置零被拒绝；主机名被拒绝。
- [x] 指纹是 SHA-256(DER)，且**有测试证明它不等于 PEM 文本摘要**、也不等于对 DER 一部分的摘要。
- [x] 比对成功前读取令牌抛 `PAIR_REJECTED`；成功后可用。
- [x] 比对失败关闭上下文，令牌**永久**不可用，且同一上下文不得重新协商。
- [x] 一个连接的失败不影响另一个连接（每连接隔离）。
- [x] 配对令牌：300 秒窗口以**单调计时**判定；单次消费；服务重启后未消费令牌全部失效；响应丢失不恢复令牌。
- [x] 失败对端**统一** `PAIR_REJECTED`，本地理由不可从线上答案到达；限流返回 429。
- [x] 限流：每来源 5 次/分钟与全局 30 次/分钟两个滑动窗口，来源间互不影响，失败随时间移出窗口。
- [x] `POST /v1/pair` 请求与响应模型严格解析；`clientLabel` 按 **UTF-8 字节**上限 128 校验。
- [x] capability 词表**未被发明**：复用 T02-02 的 `CapabilitySet`，空列表为合法答案。
- [x] 控制体严格解码：≤1 MiB、拒绝 BOM、拒绝非法 UTF-8、**拒绝重复对象键**（含转义拼写）、深度 ≤16。
- [x] 线上错误体 `{code,message,retryable,requestId?}`：`message` 由错误码的 `messageKey` 生成，调用方**无法**放入自由文本；`retryable` 必须与 §11 一致。
- [x] §11 的状态表被测试**双向**钉住；只有三个码标记为可重试。
- [x] `Authorization: Bearer` 严格解析；**不存在**从查询串读令牌的解析器。
- [x] `Retry-After` 只接受 delta-seconds（拒绝 HTTP-date）；429 必须带正数延迟；限流器据最早失败给出等待秒数。
- [x] §7 的 19 行路由建模为数据并被测试钉住；**所有 ID 参数在匹配时即按 §4 校验**，业务逻辑拿不到未校验参数。
- [x] 百分号编码在**整个请求目标**中被拒（`%2F` 改段数、`%2e%2e` 变穿越，都不必到达匹配器）。
- [x] 分页上限：files 128 / chunks 1024 / status 1024，manifest 上限**取决于 kind**；未知查询参数被拒。
- [x] §6 的「files 页不得带 fileId、chunks 页必须带 fileId」被强制。
- [x] 块偏移只从索引推导；**不存在**能让客户端 offset 进来的参数。
- [x] §6 页结构建模为 sealed 类型：files 页不得带 fileId、chunks 页必须带，由类型拒绝而非构造时检查。
- [x] 分页器同时满足条目上限与 1 MiB **字节**上限；字节上限靠**编码测量**而非估算。
- [x] 重传语义：同页重传被接受且不改变计数（按索引去重，而非 append 页）；重叠且内容不一致被拒。
- [x] `seal` 按 §6 顺序检查（缺页 → 重复 fileId → 块数量/长度 → **总摘要**），摘要放最后。
- [x] 页可乱序到达仍按索引升序 seal；`seal` 后页不可修改；30 分钟窗口从**首次内容**起算。
- [x] §7 的成功响应体建模并双向校验：ack、状态、offers、创建、块写入结果、控制轮询、授权/恢复凭证、`nextIndex`。
- [x] §10 的线上状态名从枚举常量**推导**（非第二张表），并有测试把 §10 的 16 个名字作为数据比对。
- [x] §4 的「哪些数字是字符串」按字段落实：`fileCount` 是 JSON 数字，`totalBytes`/`leaseEpoch`/`checkpointSeq`/块 `index` 是十进制字符串，互换即拒绝。
- [x] 凭证只出现在 `AuthorizationGrant` 与 `ResumeGranted`；其余响应体被测试断言**不**携带凭证；202 形态明确拒绝令牌。
- [x] §8 的帧定界规则做成纯函数：拒绝 `Transfer-Encoding`（任何取值）、压缩、两个 `Content-Length`（逗号连接或仅大小写不同）、非 `application/octet-stream`；块体必须**恰好**等于预期长度。
- [x] §8 的 `X-LFT-` 命名空间内未定义头被拒绝，命名空间外的不相关头被忽略；块响应头中的摘要是**参考性**的，不构成拒绝理由。
- [x] **修正**：内容冲突原先被报成可重试的 `DB_COMMIT_FAILED`，已映射为 §8 指定的 `CHUNK_HASH_MISMATCH`（422）。
- [x] §8 的批量 checkpoint：待持久化数据**从不超过 16 MiB**（放不下就先提交再写入）、**每 16 MiB 或 1 秒** checkpoint、**暂停/文件结尾强制提交**；`verified_pending` **不计入**已提交进度。
- [x] §8 的 `checkpointSeq` 与块标志**同事务**提交、不因空窗口递增、重试不回退；schema v3 与 v2→v3 迁移端到端验证（新库与升级库的列一致）。
- [x] §8 的**每文件串行写入**：同一 `fileId` 一次只有一个写入者，不同文件可并发；失败或解除后文件槽交还下一位。
- [x] §8 的**恢复交接顺序**：先拒绝旧世代（立即，且对已排队的写入也生效），再等待其写入者停止，然后才分配新世代；超时**抛错且不分配**。
- [x] §7 的**鉴权决定**：逐路由凭证要求表（pair 与 resume 两处例外）、三种授权作用域、**401 与 404 的分界**、`AGENTS.md` §5 强制的**操作方向**校验（违规 → 403）。
- [x] §7 的**控制响应包**：`Cache-Control: no-store` **结构性保证**（无法省略、无法被调用方覆盖、无法事后删除）、`application/json; charset=utf-8` 与 1 MiB 校验、`WireError` 首次有发出点、`requestId` 回显。
- [x] §7 的**请求管线**：路由与参数校验 → 鉴权 → 用例分派 → 结构化错误；**校验先于鉴权**；未注册路由答 404。
- [x] **第一个可用端点 `POST /transfers`**：请求体严格解析（§4 的两种数字各按各的规则）、方向限制（客户端仅可提议 `client_to_server`，违规 403 且不写）、§9 的 `requestId` 幂等（效果与记录**同事务**、重放、`REQUEST_ID_CONFLICT`）、客户端提议标识符复用的两种情形。
- [x] **修正三处真实缺陷**：`transaction` 不可嵌套（改用 savepoint，使效果可安全调用普通仓库方法）；`parseDecimalString` 与 `Content-Length` 的范围检查原先在 `int.parse` **之后**，19 位超范围值会抛出原始 `FormatException`。
- [x] **§6 的 staging 生命周期**：`PUT /manifest` 接收清单页（重传相同页返回成功）、`POST /seal` 按 §6 顺序校验并转 `WAITING_ACCEPT`（失败**不移动任务**）、**30 分钟窗口执行**（过期答 410 并丢弃提议，**已冻结的清单不受影响**）、seal 的两种幂等（`requestId` 重放 + `seal()` 自身幂等）。
- [x] `dart format`、`flutter analyze`、`flutter test` 通过；CI 四个作业通过。
- [ ] 目标端网络绑定与候选地址可达性（**阻塞于 B02**）。
- [ ] §9 的状态体与缺块区间；HTTP 服务器（socket/TLS/帧解析）与连接上的响应写出；**其余 16 个端点**（decision、offers、status、块读写、checkpoint、pause/complete/cancel、resume、control）；decision 时的空间预检持久化；staging 是否持久化（当前进程内）；协议版本协商；会话到任务的映射；§8 的多进程文件锁与背压。

## 本次结果

### `t03-01-01`：配对载荷与「pin 先于令牌」的信任上下文

已实现 `pairing_payload.dart`、`pairing_trust.dart`、`base64url.dart` 与 68 项测试
（Flutter 测试总数 358 → 426）。
详见[运行汇总](../testing/evidence/2026-09-20/t03-01-01/summary.md)。

`lib/core/security/` 目录由此建立（`AGENTS.md` §4 目录规则要求该层存在）。

**最重要的一条不变量做成结构性而非约定性**：§2 要求「必须在发送令牌、HTTP 请求及文件内容之前
完成指纹比对」。这种规则很容易写下来、也很容易写错，因为**写错的实现仍然能与诚实服务端正常工作**。
所以令牌不是一个字段，而是只能经 `PairingHandshake.pairingToken` 取得，
该 getter 在比对成功前抛 `PAIR_REJECTED`——调用方没有顺序需要记住。

**指纹的三种「形状相同但语义不同」的写法**（DER / PEM 文本 / SPKI）都有测试固定差异，
因为弄错会得到一个看似合理却永远匹配不上的 pin。

### `t03-01-02`：配对令牌生命周期与 `/v1/pair` 模型

已实现 `pairing_token.dart`、`pair_request.dart` 与 60 项测试
（Flutter 测试总数 426 → 486；安全层测试 68 → 128）。
详见[运行汇总](../testing/evidence/2026-09-20/t03-01-02/summary.md)。

**令牌生命周期**：300 秒窗口以**单调计时**判定（墙上时钟可被 NTP 或用户改时间移动，
那会延长令牌寿命或让活令牌立刻过期）；单次消费；**服务重启失效是构造性的**——
不持久化任何令牌状态，所以没有「可能被漏掉的失效步骤」；响应丢失后没有任何 API 能恢复令牌。

**只保存摘要**：§2 已为恢复密钥确立「服务端仅保存验证摘要」，配对令牌同样处理。
**诚实标注**：这一条**没有测试覆盖**（Flutter 无 `dart:mirrors`，无法断言内部字段），
由代码结构保证，已在运行汇总里明确写成「未经测试」而不是含糊带过。

**比较不声称常数时间，并说明原因**：Dart 不提供时序保证（JIT 可重排），
手写「常数时间」循环会是一种**虚假保证**。真正约束风险的是 256 位熵、单次消费、
300 秒窗口、滑动限流与「只比较摘要」。

**统一失败做成结构性**：`PairingRejection` 只用于本地诊断，
而 `wireCode` 对三种拒绝原因是**同一个常量**，测试断言这一点，并断言本地理由从线上答案不可达。

**限流在令牌之前检查**：已被限流的调用方即使拿的是**正确**令牌也只得 `RATE_LIMITED`，
因此无法靠「换令牌再试」探测哪个令牌存在（有测试覆盖）。

**capability 词表不发明**：复用 T02-02 的 `CapabilitySet`，测试全程用合成标识符。

**期间发现一个协议内在张力（已登记，未修）**：§9 要求 `requestId` 幂等重放，
但 §3 要求配对响应丢失后**不恢复**已消费令牌——配对是 §9 幂等的一个例外。
本层只校验 `requestId` 格式，**没有**实现任何幂等重放语义，因为对配对而言重放正是 §3 禁止的事。

### `t03-01-03`：`/v1` 线上契约层

已实现 `json_body.dart`、`wire_error.dart` 与 79 项测试
（Flutter 测试总数 486 → 565；协议层测试 124 → 204）。
详见[运行汇总](../testing/evidence/2026-09-20/t03-01-03/summary.md)。

**控制体不能只交给 `jsonDecode`**：Dart 的解码器**不拒绝重复的对象键**（保留最后一个、什么也不说），
这正是解析器差分的形状——两个实现读同一批字节、得出不同的值。
所以先扫描一次结构（深度、重复键、转义）再取值；扫描器**刻意不构建值**，
因为两个都构建的解析器就是两次产生分歧的机会。

**重复键可以藏在转义里**：`{"a":1,"\u0061":2}` 是同一个键的两种拼写。
比较原始文本会判为合法，所以扫描器**先解码转义再比较**（含代理对）。
另外，重复键的**报错不回显键名**——键来自请求体，而控制体可以携带令牌。

**错误体的 `message` 不是自由文本**：`WireError.of(code)` 不接受 message 参数，
只写入该码的 `messageKey`（封闭常量集），所以调用方传什么都到不了这个字段。
这把 T02-02 那句注释（"A key rather than a string: §7 requires the message to carry no secret"）
变成了结构。**收到的** message 不同：对端可发任意文本，只限长并查控制字符，
**不**猜测它是否含密钥（那种启发式只会带来虚假的安心）。

**`retryable` 是校验而非采信**：一个发来 `retryable: true` + `CHUNK_HASH_MISMATCH` 的对端
等于邀请客户端**永远重试一个损坏的块**，所以解析值必须与 §11 的行为一致，两个方向都测。

**§11 的表被双向钉住**：表中每个码的状态必须一致，且枚举里不能有表中未出现的码。

**`Retry-After`**：只接受 delta-seconds，**拒绝 HTTP-date**（离线时钟正是协议别处拒绝信任的东西）；
429 必须带正数延迟；限流器新增 `retryAfterSeconds`，从**最早的失败**何时移出窗口推导，
而不是窗口长度——在 20 秒就够的时候让客户端等满一分钟是另一种错。

### `t03-01-04`：§7 路由表与参数校验

已实现 `api_routes.dart` 与 38 项测试（Flutter 测试总数 565 → 603；协议层 204 → 241）。
详见[运行汇总](../testing/evidence/2026-09-20/t03-01-04/summary.md)。

**§7 的开篇规则「所有 ID 参数须先解析校验」被放在匹配阶段**：路由匹配在校验之后才返回，
所以业务逻辑**拿不到**未校验的参数——它拿到的是类型化的 `MatchedApiRequest`。
19 行建模为**数据**，并由测试钉住（新增/删除/改方法都会失败）。

**百分号编码在整个请求目标中被拒**：本 surface 携带的每个值都是未转义的规范形式，
所以正确的请求从不需要转义，而拒绝它去掉了 `%2F`（改段数）与 `%2e%2e`（变穿越）这两类绕过。
**推论**：游标必须免转义——这是对 §7 留白格式的约束，已登记待确认。

**未知方法答 `NOT_FOUND`**：§11 没有 405，`AGENTS.md` §3 禁止发明协议细节，
所以采用 §7 已写下的「未知/未授权统一 404」。这是**解读**，已登记待确认。

**块偏移只从索引推导**（§8）；**不存在**能让客户端 offset 进来的参数——测试直接断言 `?offset=` 被拒。

**分页上限取决于 kind**（files 128 / chunks 1024 / status 1024），
且 §6 的「files 页不得带 fileId、chunks 页必须带 fileId」在 `manifestKind()` 中强制。

**期间由测试发现两个缺陷**：① 路由模板的前导空段使**每条路由都匹配失败**（15 项测试同时报错），
原因是模板与请求路径切分形状不一致；② kind 相关上限的哨兵值落穿，`kind=files` 时返回 `-1` 而非 128。
两个都是测试先发现、而非审阅先发现。

### `t03-01-05`：清单分页与 seal

已实现 `manifest_page.dart`、`manifest_staging.dart` 与 50 项测试
（Flutter 测试总数 603 → 653；协议层 241 → 292）。
详见[运行汇总](../testing/evidence/2026-09-20/t03-01-05/summary.md)。

**页结构是 sealed 类型**：§6 规定 files 页不得带 fileId、chunks 页必须带，且两种页的 items 是不同记录。
用一个类加可选字段就允许两种被禁止的组合；sealed 让类型本身拒绝它们。
页内**索引不是自由的**：「每页覆盖连续索引区间」意味着第 i 项索引必须等于 `startIndex + i`。

**字节上限靠编码测量而非估算**：估算要同时把 JSON 转义、十进制字符串字段与多字节字符都建模对；
把候选页真的编码出来量一次，在这些方面都无法出错。
**一个诚实的发现**：按 §5.1 的 1024 字节路径上限，现实输入下**条目上限总是先起作用**，
字节上限是安全网——测试用最大合法路径填满 128 项断言一页装得下，
另用发送方不可能构造出的超大条目驱动机制本身。

**重传由内容决定，而不是由区间决定**：§6 拒绝的是「重叠区间**内容不一致**」，
把规则写成「拒绝重叠区间」会拒绝只是换了边界的诚实重传。
**按索引存储是让「不能用重复页增加累计数量」成立的结构**——
append 到列表的实现能通过天真的重传测试，却仍会把重复页算两次（有测试断言三次发送后计数不变）。
乱序到达也能正确 seal，因为页的 `startIndex` 就是承载 §5 逻辑顺序的东西。

**seal 按 §6 的顺序检查，摘要放最后**：摘要对每一种问题都会失败，却对「是哪一种」一言不发，
所以先报「file index 1 is missing」。摘要检查复用 T02-01 的 `FrozenManifest.verifyDigest`
（其注释里当初就写了「§6 seal」）；块长度检查复用 `chunkLengthForIndex`，
使对端**不能**靠声明短块跳过字节。

**30 分钟窗口只回答问题，不执行清理**：`isExpired` 是谓词；清理动作属于拥有那些状态的服务端。
窗口从**首次内容**起算，否则一个还没开始上传的提议会在客户端仍在决定时过期。

### `t03-01-06`：`/v1` 响应体契约

已实现 `api_responses.dart` 与 49 项测试，并补上 §10 的线上状态名与十进制字符串的**编码端**
（Flutter 测试总数 653 → 702；协议层 292 → 341）。
详见[运行汇总](../testing/evidence/2026-09-20/t03-01-06/summary.md)。

**§4 决定哪些数字是字符串**（这一点极易读错）：字节数、块编号、`chunkCount`、`leaseEpoch`、
`checkpointSeq` 是**十进制字符串**；协议版本、`chunkSizeBytes`、`length`、`fileCount`、
`pageLimit` 是 **JSON 整数**。所以 `totalBytes` 是字符串而 `fileCount` 是数字，
互换的响应**对端读不出来**。每个模型内部存 `int`、在边界转换，并有测试直接断言类型。
顺带补上 `encodeDecimalString`：负数与超范围值在 §4 下**没有合法编码**。

**§10 的状态名从枚举常量推导**而不是维护第二张表——改名的常量会同时改变两者，
也不存在「忘了更新表」。测试把 §10 写的 16 个名字作为**数据**逐一比对；
`fromWireName` 对未定义名字返回 **null 而不是默认值**（猜错状态正是两实现在「是否可续传」上分叉的方式）。

**凭证只出现在两种响应里**（§7）：做成 `TokenBearingResponse` **接口**，
只被 `AuthorizationGrant` 与 `ResumeGranted` 实现，并有测试断言其余响应体**都不是**它。
202 形态**明确拒绝令牌**——§11 说用户确认不应占用长期请求，在 202 里发令牌等于
**在工作完成前就交出凭证**。

**期间修正一处自己的不一致**：`ResumeGranted.expiresInSeconds` 原先不做正数校验，
而 `PairResponse` 会做。已统一并补测试——这属于本实现的不一致，不是 §7 允许它。

### `t03-01-07`：§8 请求头与体规则

已实现 `chunk_headers.dart` 与 40 项测试（Flutter 测试总数 702 → 742；协议层 341 → 381），
并修正一处跨层的错误码缺陷（见下）。详见[运行汇总](../testing/evidence/2026-09-20/t03-01-07/summary.md)。

**为什么做成纯函数**：§8 的这些是**帧定界规则**，而请求走私就住在帧定界里。
放进「对收到时的头映射求值的纯函数」，判定与「读 body」彻底分离
（**帧不合法的消息一个字节都不会被处理**），且**不需要 socket 就能完整测试**。

**「单一 Content-Length」在头映射里意味着什么**：头映射无法把同名放两次，
所以重复会以两种形态出现，**两种都拒绝**——只差大小写的两个条目
（HTTP 字段名大小写不敏感，**悄悄合并就会替实现选一个长度**），
以及逗号连接的一个值（那是对端在**说存在两个长度**）。

**只有本项目自己的命名空间被拒绝**：HTTP 带着大量无关头，全部拒绝是错的；
但 `X-LFT-` 前缀是我们自己的，那里的未定义头被拒绝（理由同 §4 拒绝未定义 JSON 字段）。

**冻结清单是权威**：`agreesWithManifest` 报告对端的摘要是否与清单一致，
**但不把不一致变成拒绝**——在参考性字段上拒绝，等于把一个对端本来影响不了的失败权交给它。

**期间发现并修正一个真实缺陷**：§8 要求内容冲突返回 `CHUNK_HASH_MISMATCH`，
并说「不可因『已有块』就无条件成功」。追查后确认**校验本身是有的**
（与**冻结清单**比对，权威来源正确），**但错误码是错的**：
`syncReceiptMismatch` 当时**没有线路码映射**，于是 `toProtocolError()` 的兜底分支
把它报成 **`DB_COMMIT_FAILED`（500，可重试）**——**邀请客户端无限重发一个内容冲突的块**，
正是 §8 与 §11 各自禁止的事。已映射为 `CHUNK_HASH_MISMATCH`（422，不可重试）并补测试。
这个缺陷是**把 §8 的文字与存储层实际行为对照**才发现的：两边测试各自都是绿的。

### `t03-01-08`：§8 批量 checkpoint 窗口与 `checkpointSeq`

已实现 `commit_window.dart`、schema v3（`tasks.checkpoint_seq`）与存储层的批量提交路径
（Flutter 测试总数 742 → 782；窗口 23 项 + 存储 14 项 + 迁移 3 项）。
详见[运行汇总](../testing/evidence/2026-09-20/t03-01-08/summary.md)。

**为什么需要窗口**：§8 允许用 `verified_pending` 而不是 `committed` 回答块 `PUT`，
因为字节已落盘并验证但**没有任何块行被提交**，断点没有前进。这是有意的取舍——
每块单独提交是每 4 MiB 一个事务，批量提交的代价是崩溃时丢掉上次 checkpoint 之后的工作。
§8 用两道界限框住损失（**16 MiB**、**1 秒**）并在**暂停/文件结尾**强制提交。

**窗口里有意的「没有」**：**没有第三种块状态**。`chunks.state` 仍是 `missing`/`committed`，
因为「已写但未提交」不是进度（`AGENTS.md` §2 规则 5），
一个 `verifying` 状态会成为「我们到哪了」的第二个答案。`verified_pending` 是**响应**而非存储状态，
所以窗口中途崩溃会把块留在 `missing`，它们只是被重传——测试直接钉住这一点。

**窗口持有「收据」而非索引**：批量提交在**事务内**把每张收据与冻结清单再比对一次。
只记索引会让批量提交变成一条「把任何已登记块标成已提交而无需证据」的路径。
摘要冲突使**整批**失败、一条都不提交、序号不前进（半应用的 checkpoint 会谎报进度）。

**「单一提交路径」被保留而不是被并列**：`commitChunkAfterSync` 现在**是**
`CommitWindowPolicy.immediate`（容量=一个块、间隔=0）下的同一个窗口路径，
所以标记 `state='committed'` 的 SQL **只有一处**。立即策略必须用**零间隔**表达——
尾块小于一个块，只靠容量判断它永不到期，于是每个文件的最后一块都会停在 `verified_pending`。

**`checkpointSeq` 落在 `tasks`**：它的搭档 `lease_epoch` 在那里，而 V2.1 把
`checkpoint_seq` 放在本仓库并不存在的 `sessions` 表里；该偏离已登记。
**作用域选每任务**（§8 未点名），理由同上。v3 用 `ADD COLUMN ... NOT NULL DEFAULT 0`，
`applyVersion1`/`applyVersion2` 一字未改，并再次自动验证「新库与升级库的列完全一致」。

### `t03-01-09`：§8 写入栅栏（每文件串行 + 恢复交接）

已实现 `write_fence.dart` 与 21 项测试（Flutter 测试总数 782 → 803），
并把栅栏接到仓库上（`writeChunkWithFileLock`、`revokeAndAdvanceLeaseAfterWritesStop`）。
详见[运行汇总](../testing/evidence/2026-09-20/t03-01-09/summary.md)。

**已有的世代检查为什么不够**：存储层已能在提交事务内再查一次世代，让被取代的写入
**无法提交**——但没让它无法**写入**。缺的两个都关于时间：
（1）没有东西把同一文件的两个写入者串行化，两者的字节会交错写进同一个暂存文件，
而输的一方在提交被拒时**字节早已落盘**——**已提交的块行仍然是对的**，这正是危险处；
（2）`revokeAndAdvanceLease` 只递增世代不等待，一个正在 sync 中途的写入会继续写进
新一代即将使用的文件。

**顺序即保证**：先**拒绝**旧世代的后续写入（立即），再**等待**它的写入者全部停止，
然后才分配新世代。若只在世代变化时才拒绝，等待期间会冒出新的旧世代写入，等待永不结束。
排队的写入也在拿到文件槽**之前**就登记，故 drain 也等它；
而拿到槽之后**再查一次**世代，不通过就一个字节都不写——这一步是必需的，
因为一个写入可能先通过检查、排在别人后面，等排到队首时恢复已经接管。

**在途写入被允许写完并提交**：中止它只留下半写的暂存文件而无好处，
而 §8 下一步「**校验已有数据**」正是让旧世代下提交的块可以保留的原因。
栅栏保证写入**停止**，不保证它**被撤销**。

**超时不得被读成「写入已停止」**：`timeout` 到期**抛错**且**不分配**世代，
世代保持被撤销以便重试——把超时当「停了」的调用方会在旧写入仍在写文件时分配新世代。

**没有重复上一批的「孤儿模块」问题**：仓库**持有**栅栏（各造一个栅栏等于没有栅栏），
`writeChunkWithFileLock` 是「写入顺序 + 文件锁」的一次调用，
`revokeAndAdvanceLeaseAfterWritesStop` 把三步做成一次调用。

**用变异验证测试不是空的**：三处临时变异分别导致 **4 / 3 / 1** 项失败
（去掉串行化、drain 永不等待、去掉二次检查），随后恢复原文并重新确认全部通过。

### `t03-01-10`：§7 请求管线、鉴权决定与控制响应包

已实现 `transfer_direction.dart`、`control_message.dart`、`control_authorization.dart`、
`control_pipeline.dart` 与 85 项测试（Flutter 测试总数 803 → 888），
`lib/core/network/` 目录由此建立。详见[运行汇总](../testing/evidence/2026-09-20/t03-01-10/summary.md)。

**补上的是一直没人实现的两条 §7 规则**：「所有控制响应 `Cache-Control: no-store`」
与「错误体 `{code,message,retryable,requestId?}`」——后者此前只做了一半：
`WireError` **会**生成与解析错误体，但**没有任何地方发出它**，因为「写响应」这一层不存在。
连同「除 pair 外均需要有效 Bearer」的鉴权决定（台账登记的剩余项）一并实现。

**`no-store` 做成结构性而非约定性**：理由与 `WireError.of` 不接受 message 相同——
**一条每个调用点都要记得的规则，会在最要紧的那一次被忘掉**（携带令牌的那个响应）。
故没有「不带 no-store」的构造方式、没有 `raw`；工厂把它放在展开调用方头**之后**最后写入，
所以调用方自带 `Cache-Control` 会**输给** §7（有测试断言），头映射也不可事后修改。

**鉴权里三件事必须分开**：（1）哪条路由要什么凭证（逐路由要求表，键为路由名）；
（2）凭证的作用域（会话 / 任务令牌 / 受限完成查询令牌）；（3）**不够格时答什么**。
第三条的两个分支最要紧：**令牌不覆盖该资源 → 404**（§7「任务查询对无权限资源统一 404」，
否则状态码成了枚举 transfer id 的工具），**方向违规 → 403**（资源存在且可见，但操作不属于该方向）。
两者混淆会让「不存在的资源」与「用错方向的资源」看起来一样。

**要求表缺失即响亮失败**：`assertCoversEveryRoute()` 每次构造管线时断言表与 `ApiRoutes.all`
完全一致；新路由没有要求会**抛错**，而不是**默认匿名**——默认成「无需凭证」会把新端点变成开放端点。

**`resume` 是第二条例外**：§7「除 pair 外均需要 Bearer」紧接着说 resume 用专用请求体、
初次 resume 无须旧会话令牌，故管线**不向它索要 Bearer**（否则第一次恢复永远无法发生），
并建模为单独的 `resumeBodySecret` 而非 `none`，以免被误当匿名路由。

**方向校验是 `AGENTS.md` §5 的强制要求**（「所有文件请求先验证任务授权、**操作方向**、…」），
§7 逐行点名了方向。方向值做成**枚举**而非裸字符串：`String` 无法穷尽检查，
比较里的拼写错误会照常编译并按不相等处理。

**管线逐项说明哪些阶段有、哪些没有**（而不是含糊带过）：TLS/pin 属传输层、
协议版本协商属配对握手且 §7 其余行不带版本字段、`request_id` 幂等需另行接线——
三者**明确未做**；授权、参数校验、用例分派、结构化错误**已做**。
**校验先于鉴权**：畸形 target 即使同时也没有令牌也答 400，
因为路径形状是公开的，§7 的「统一 404」保护的是**资源**而非**语法**。

**用变异验证关键性质真的被断言**：把 `no-store` 放到展开调用方头之前 → **1 项失败**；
把「令牌不覆盖该 transfer」从 `notFound` 改成 `directionForbidden` → **2 项失败**。随后均已恢复原文。

### `t03-01-11`：`POST /transfers`（第一个可用端点）与三处真实缺陷

已实现 `transfer_creation_request.dart`、`credential_fingerprint.dart`、
`transfer_creation_handler.dart` 与 78 项测试（Flutter 测试总数 888 → 957），
并修正 **三处真实缺陷**（其中两处是同一类）。
详见[运行汇总](../testing/evidence/2026-09-20/t03-01-11/summary.md)。

**「管线可用，端点未实现」到此为止**：上一批结束时处理函数表是空的、每个请求答 404；
本批放进第一个用例（§6「客户端为发送者：`POST /transfers` 创建 staging 任务」），
服务端**真的会创建一行 `STAGING` 任务**，且 21 项测试检查**数据库**而不只是响应。

**让这件事不只是「一次插入」的两条规则**：方向（§7「客户端仅可提议 `client_to_server`」，
违规答 403 且**一行都不写**）与 `requestId`（§9「持久化请求摘要和结果；相同 ID 不同参数拒绝
`REQUEST_ID_CONFLICT`」）。后者不是可以后补的记账：没有它，丢了响应的客户端重试时，
服务端**无法把重试与第二个传输区分开**。故**效果与幂等记录在同一事务**里由
`executeAtomically` 写出——这是两者不可能不一致的唯一安排。

**客户端提议标识符带来第三种情形**：*不同* `requestId` 携带*已存在* `transferId`。
声明完全相同 → **不写任何东西而返回成功**（客户端多半丢了 `requestId`；§6 对重传清单页同此）；
声明不同 → **409 `INVALID_STATE`**——静默覆盖会毁掉另一个传输可能仍在用的任务。

**请求摘要按解析后的字段算而非原始字节**：否则字段顺序不同但语义相同的重试会被判为冲突，
把合法重试变成 `REQUEST_ID_CONFLICT`。摘要走既有 `CanonicalWriter`（定宽，无需分隔符），
`requestId` 刻意不在其中（它是键不是参数），方向字节按**线上值**写以免重排枚举改变已存摘要。

**缺陷一：`transaction` 不能嵌套。** 端点第一次运行**每一项都失败**并报
`NS-STORAGE-006: transaction rolled back`——效果调用 `registerTask`，而它自己又开了一个事务，
`BEGIN IMMEDIATE` 在事务内被 SQLite 拒绝，再被 `_classify` 包成一句看不懂的回滚。
**这不是罕见形状，这正是每个幂等端点的形状**：§9 要求请求记录与效果同事务，
而效果天然会调用普通仓库方法。故修**这一类**：嵌套改用 **savepoint**，
`readTransaction` 在已开事务内直通。语义两半都必须对：**内层失败只回滚自己那层且外层仍可提交**，
而**外层失败依旧丢弃全部**——只做对前一半比不支持嵌套更糟，因为「部分应用的事务」
正是事务存在的意义。`ROLLBACK TO` 会把 savepoint 留在栈上，故必须再 `RELEASE`（有测试）。

**缺陷二与三：`int.parse` 在范围检查之前（同一类）。** 新端点的测试发现
`parseDecimalString('9223372036854775808')` 抛的是**原始 `FormatException`**，不是 `INVALID_DECIMAL`。
§4 的模式允许最多 **19 位**而 2^63-1 也是 19 位，所以「形状合法但超范围」的值**真的会到达解析器**；
原有测试用 **20 位**值，在更早的位数检查就被拦下、**从未走到解析器**，所以一直没被发现。
**影响是安全相关的**：任何经它解析的对端字段（块索引、`totalBytes`、`startIndex`、`limit`、
`afterSeq`、`leaseEpoch`、`checkpointSeq`、`seq`…）都能被对端用一个 19 位数字触发未处理异常，
把一次校验失败变成一条崩溃路径。顺着同一模式又找到**第二处**：`chunk_headers.dart` 的
`_parseContentLength`（第十八批所写）只校验「全是数字」而无位数上界。
**修法是一处定义**：`parseNormalizedDigits` 承载「按比较而非解析判范围」，两处都走它；
`Content-Length` 还**先剥前导零再判范围**，使长串前导零这种合法 HTTP 写法依旧可用。
两处缺陷都是**先观察到失败再修**的，不是事后补测试。

### `t03-01-12`：清单页写入与 seal（§6 的生命周期）

已实现 `transfer_seal_request.dart`、`manifest_staging_registry.dart`、
`transfer_staging_endpoint.dart` 与 40 项测试（Flutter 测试总数 957 → 997）。
详见[运行汇总](../testing/evidence/2026-09-20/t03-01-12/summary.md)。

**本批几乎没有新的协议逻辑，这是有意的**：`ManifestPage` 已校验页的形状与 §5.1 上限，
`ManifestStaging` 已按索引存储、拒绝内容不一致的重叠区间、按 §6 顺序 seal（总摘要最后）。
本批加的是**只有端点才能负责**的部分——按路径找到正确的 staging、执行撤销窗口、
把 seal 变成持久状态变更并在失败时**不移动任务**；**没有把已有 staging 测试抄一遍**。

**§6 的 30 分钟窗口终于有了执行者**：`isExpired` 此前只是谓词、无人调用。
现在在使用点执行：过期提议答 **410 `TASK_EXPIRED`** 并**丢弃**该提议，
而**一旦冻结就不再适用**——正是那句话强调的后半句。两半都有测试
（过期后 `stagedTransferCount` 归零；**已 seal 的在窗口过后仍可用**）。
另固定「窗口从**首次内容**起算」：从未上传过的传输不因创建得久而过期。

**两条绑定在 §6 上的规则**：**重传是成功而非错误**（丢了响应的客户端无法分辨两种情况，
把第二次答成错误会让一次响应丢失不可恢复）；**seal 的两种幂等**——§9 的 `requestId` 重放，
以及 `seal()` 自身的幂等（必需，因为冻结**一半在内存里**：数据库事务在冻结后失败时，
重试必须仍能完成，而不是发现一个再也 seal 不了的清单）。

**一处自己发现的判断错误**：第一版把「摘要与声明不符」的检查放在 `executeAtomically`
**之前**，于是「同一 `requestId` 换摘要」被答成 `MANIFEST_MISMATCH`——
但 §9 说那正是 **`REQUEST_ID_CONFLICT`**。改为放进**效果内部**后三种情形各得其所：
新 ID + 摘要不符 → 422；同 ID + 摘要不同 → 409；同 ID + 摘要相同 → 200 重放。

**「不移动任务」被单独验证**：§6「不能进入 WAITING_ACCEPT」，所以每条 seal 失败路径的测试
都**额外断言 `taskState` 仍是 `staging`**，而不只是断言错误码。

**期间发现一处存储层与线上层的名字差异**（复核项）：`tasks.state` 存的是 **Dart 枚举名**
（`staging`）而线上是 **`wireName`**（`STAGING`）。`_parseTransferState` **拒绝**未识别值（对的），
所以测试里写成线上名立刻失败。两套名字各有用途，但**手写状态字符串的地方会踩坑**，已登记。

## 仍剩余（任务保持「进行中」）

1. **目标端网络绑定与候选地址可达性（阻塞于 B02）**：指定 Network、多网卡、地址是否真的在本地链路。
   本任务只做词法校验，**不声称**任何可达性结论。
2. **§9 的状态体未建模**：`{transferId,manifestDigest,state,leaseEpoch,checkpointSeq,committedBytes,fileId?,missingChunkRanges?,snapshotId?,nextCursor?}`
   连同缺块区间校验（两端包含、递增、不重叠、不越界、每页 ≤1024）。
   **其线上形状有一处草案留白**（「十进制字符串对」是二元素数组还是对象未定），故本次故意未实现。
3. **§8 的写入顺序与背压仍有缺口**：「最多 16 MiB 待持久化有效数据」、「每 16 MiB 或 1 秒 checkpoint」、
   「暂停/文件结尾强制提交」**已实现**（`t03-01-08`），
   **每文件串行写入**与「先撤销旧会话、**等待旧写入停止**、再分配新 epoch」
   也已实现（`t03-01-09`，`WriteFence` + `writeChunkWithFileLock` +
   `revokeAndAdvanceLeaseAfterWritesStop`）。仍缺：
   - 顺序里的「**认证和授权**」一步属端点层，仍需 HTTP 层；
   - `writeChunkThroughWindow` 可被直接调用从而**绕过文件锁**——持锁是**方便**路径而非唯一路径；
   - 栅栏是**进程内**的，**多进程**打开同一数据库时无仲裁（需平台文件锁）；
   - `revokeAndDrain` 默认**无限等待**，只有调用方传 `timeout` 才有上界；
   - **背压**（§9「接收端磁盘速度通过背压限制发送端」）**完全未实现**——
     窗口状态已可读，但没有任何东西在读它来放慢发送端；
   - **批量窗口未做真实磁盘测量**，16 MiB 与 1 秒只按字节数与时钟判定，
     「同一文件」在真实平台（SAF / 安全作用域资源）上是否等于「同一 `fileId`」也未验证
     （与 T04-01 的「批量 checkpoint 窗口与背压未测量」是同一件事）。
4. **19 个端点的实际逻辑实现了 3 条**：`POST /transfers`（`t03-01-11`）、
   **`PUT /manifest` 与 `POST /seal`（`t03-01-12`）** 已可用，
   路由能匹配、请求与响应体都能编码解析、**鉴权决定与响应包都已实现**（`t03-01-10`），
   但其余 **16 行仍答 404**。正确描述是「**3 个端点可用，其余 16 个未实现**」——
   不得读作「服务可用」。仍缺**decision**、**offers 分页**、**status**、**块 PUT/GET**、
   **checkpoint**、**pause/complete/cancel**、**resume** 与 **control**。
   §6 的 30 分钟 staging 窗口**已由 `t03-01-12` 执行**（过期答 410 并丢弃提议，
   冻结尾的清单不受影响）；**staging 仍是进程内的**（重启后需重传并重新 seal，
   §6 让重传安全，但本 build **不得声称传输能跨服务端重启存活**）；
   `fileCount`/`totalBytes` **不落库**（它们是客户端的**声明**，权威在 sealed 清单，§7 的
   `GET /offers` 从那里读摘要；创建时只做 §5 上限校验并计入请求摘要）。
5. **没有 HTTP 服务器或客户端**：没有连接处理、没有 TLS、没有 socket 上的请求体读取与响应写出。
   管线接收的是**已经成帧**的 `ControlRequest`，所以 §7 的 `Cache-Control: no-store`
   与错误体现在**已实现并有测试**（`t03-01-10`），把它们接到连接上是 HTTP 层的事。
6. ~~**`POST /transfers` 与 staging 生命周期**（STAGING → seal → WAITING_ACCEPT）未实现~~
   **已实现**（`t03-01-11` 创建、`t03-01-12` 清单页与 seal，含 30 分钟窗口）。
   仍缺：**decision**（接收方批准）与**批准时的空间预检持久化**（§6「批准清单摘要、保存位置
   以及空间估算一起持久化」）。
7. ~~**没有鉴权决定**~~ **已实现**（`t03-01-10`）：逐路由的凭证要求表（含 §7
   「除 pair 外均需要有效 Bearer」与 resume 的 body secret 例外）、三种授权作用域、
   **401 与 404 的分界**、以及 `AGENTS.md` §5 强制的**操作方向**校验。
   仍缺：**会话到任务的映射未建模**，故会话身份在任务作用域路由上被**保守拒绝**（答 404，
   即能力缺口而非安全缺口）；`request_id` 幂等与**协议版本协商**两个管线阶段未接线。
8. **会话访问令牌的签发与交付**：`sessionAccessToken` 目前由调用方提供；
   恢复凭证与 receipt 交付（§3）未实现。
9. **TLS 引擎集成**：真正取得叶证书 DER 并驱动 `PairingHandshake`；
   离线 pin 模式下的证书日期与主机名策略（§2 要求由目标引擎原型明确）。
10. **二维码生成与扫描 UI**（T05），以及相机 / 本地网络权限路径。
11. 限流状态目前仅在进程内，重启清零——§3 未要求持久化，但真机实现时需确认该读法。
12. **暂存持久化**：`ManifestStaging` 是进程内的，服务端重启需客户端重传（重传幂等故安全）。
    协议未说明 staging 是否必须持久，**需确认**。

## 必做测试

覆盖 `QUALITY_AND_ACCEPTANCE.md` §3「安全」矩阵：

| 场景 | 期望 |
| --- | --- |
| 正确的指纹 | 比对成功，令牌可用 |
| 错误/变化的指纹 | 关闭连接，令牌永久不可用 |
| 差一个字节的证书 | 视为不匹配 |
| 未比对就取令牌 | 抛 `PAIR_REJECTED` |
| 比对失败后再次比对 | 抛 `PAIR_REJECTED`，不得重新协商 |
| 一个连接失败 | 另一连接不受影响 |
| 指纹写成 PEM 摘要 | 有测试证明与协议指纹不同 |
| 大写指纹 / 非 hex / 长度不符 | 拒绝 |
| 令牌带填充 / 非规范写法 / 标准字母表 | 拒绝 |
| host 带 scheme、凭证、路径、查询 | 拒绝 |
| 未定义字段 | 拒绝，不静默丢弃 |
| 令牌在窗口内 / 到点 | 接受 / 拒绝 |
| 同一令牌第二次消费 | 拒绝，且与未知会话、错误令牌在线上不可区分 |
| 服务重启后消费旧令牌 | 拒绝（未持久化任何令牌状态） |
| 失败达每来源或全局上限 | 429 `RATE_LIMITED`；正确令牌同样被拒 |
| 失败移出窗口 | 不再限流 |
| `clientLabel` 64 个双字节字符 / 65 个 | 接受 / 拒绝（按字节而非字符计量） |
| capability 列表含重复或空格 | 拒绝；空列表接受（词表未定义） |
| 控制体带 BOM / 非法 UTF-8 / 超 1 MiB | 拒绝（超限为 413 `BODY_TOO_LARGE`） |
| 控制体含重复对象键（含 `\u0061` 拼写） | 拒绝，且报错不回显键名 |
| 控制体深度超过 16 | 拒绝 |
| 错误体 `retryable` 与 §11 不符 | 拒绝（两个方向） |
| 错误体含未知字段或未知 code | 拒绝 |
| §11 表 | 表中每个码的状态一致；枚举无表外码；仅三个码可重试 |
| `Authorization` 缺 scheme / 空令牌 / 多余空白 | 拒绝 |
| `Retry-After` 为 HTTP-date 或缺失 | 拒绝（429 必须带正数秒） |
| 按 `retryAfterSeconds` 等待后 | 限流确实解除 |
| §7 的 19 行 | 每条都在注册表里，方法正确，无冲突 |
| 未知方法／路径 | `NOT_FOUND`（§11 无 405） |
| 路径含 `%2F` / `%2e%2e` | 拒绝 |
| ID 参数非规范 UUID | 拒绝 |
| 块索引含前导零／负号／非数字 | 拒绝 |
| 未知或重复的查询参数 | 拒绝 |
| `kind=files` 带 fileId / `kind=chunks` 缺 fileId | 拒绝 |
| `limit` 超过该端点上限 | 拒绝 |
| 客户端传入 offset | 无处可传（参数被拒） |
| 同一页发送三次 | 计数不变（按索引去重） |
| 重叠页内容一致 / 不一致 | 接受 / 拒绝 |
| 缺页 / 重复 fileId / 块长错误 | seal 失败，且错误指出是哪一种 |
| 总摘要不一致 | seal 失败 |
| seal 后再送页 | 拒绝 |
| 页乱序到达 | 仍按索引升序 seal |
| 最大合法路径填满 128 项 | 一页装得下（条目上限才是起作用的约束） |

**本任务使用纯 Dart**；真实 TLS 引擎、真实网络绑定与真实设备上的可达性属 B02/B03 真机验证，
不得用本任务结果代替。

## 交付物

- `lib/core/security/`：配对载荷、信任上下文
- `lib/core/protocol/`：base64url 规范编码、配对相关协议限额
- `test/core/security/`：载荷与信任测试
- 证据：`docs/testing/evidence/2026-09-20/t03-01-01/` … `t03-01-06/`

## 台账回填

- `docs/PROJECT_LEDGER.md`：§4（T03 行）、§4.1（T03-01 状态）、§5、§7。
