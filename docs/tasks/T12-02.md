# T12-02 共享 UI 组件

状态：**待验证**
依赖：T12-01
依据：`docs/ui/STYLE_GUIDE.md` §4、§7；`docs/ui/UI_UX_SPEC.md` §6

## 目标

在 `lib/app/widgets/` 建立按钮、Banner、状态、文件、阶段、空间、任务和空态组件。

## 退出门槛

- [x] 组件接收领域状态/展示模型，不接收任意语义颜色。
- [x] 覆盖基础状态、loading、Light/Dark、长文案和窄屏结构。
- [ ] 组件 Widget 测试通过。当前 Dart SDK 版本低于项目约束，测试尚未运行。

## 人工重点复核

按钮破坏性动作、状态图标与文字双重表达、屏幕阅读器标签。
