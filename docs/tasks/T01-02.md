# T01-02 CI 与检查

- 基线分支/SHA：`master` @ `af5eb486f3f257be82c7074f94a4f58f7dc10b18`
- 负责人或 Agent：开发 Agent（DeepSeek Harness）
- 状态：就绪 → 进行中
- 依赖：T01-01（已完成并合并，PR #4 `5a3d840` / PR #5 `af5eb486`）
- 分支：`feat/t01-02-ci-checks`
- 关联规格：
  - `AGENTS.md` §3 技术栈清单（构建发布：依赖锁定、可复现 CI 构建）、§7 必做检查
  - `docs/跨平台离线文件互传系统技术方案_V2.1.md` §13（T01 行）、§17.2 项目材料与 AI 工作约定、§17.3 安装升级与诊断
  - `docs/testing/QUALITY_AND_ACCEPTANCE.md` §2 测试层级、§7 CI 建议门禁
  - `docs/DEVELOPMENT_WORKFLOW.md` §6 PR 与门禁、§8 M1 工程基线
  - `docs/architecture/APP_AND_SERVICE_DESIGN.md` §12 依赖选择门槛
- 风险级别：普通（不涉及协议字节编码、安全凭证、SQLite 事务或删除路径）

## 目标

把 T01-01 建立的一堆手工命令固化成**每次提交都会自动执行的门禁**：任何人（或 Agent）推送代码后，
格式化、静态分析、测试、协议固定向量、相对链接、敏感信息和两端构建都会在同一套流水线里跑一遍；
没有 Flutter 工程时必须记为未执行而不是假报通过。

同时补上行尾与二进制文件的显式策略，避免 Linux CI 与 Windows 工作区之间出现行尾误报。

## 范围内

- `.github/workflows/ci.yml`：
  - `repo-checks`：协议固定向量（`tooling/s0`）、Markdown 相对链接、敏感信息、依赖锁文件已跟踪。
  - `flutter-checks`：`flutter pub get` → `dart format` 校验 → `flutter analyze` → `flutter test`。
  - `build-android`：构建 APK（debug 与 release）。
  - `build-windows`：构建 Windows release。
- 固定 Flutter 版本并**校验下载包 SHA-256**，不使用未评估的第三方 Action 来安装 Flutter。
- `tooling/checks/` 下用 Python 标准库实现跨平台的相对链接检查与敏感信息检查（无第三方依赖）。
- `tooling/scripts/check.ps1` 增加这两项，使本地与 CI 检查一致。
- `.gitattributes`：明确文本行尾与二进制文件类型。
- 依赖锁定与版本追踪：校验 `pubspec.lock` 已被跟踪，并校验 CI 使用的 Flutter 版本与本地记录一致。
- 更新 `tooling/scripts/README.md` 与台账。

## 范围外

- 签名、渠道包、发布作业与密钥托管（T10）。
- 真机测试、热点/网络探针（B01–B08）。
- 任何协议、存储、传输、UI 业务功能。
- 引入第三方依赖或升级依赖。
- 全仓格式化或无关重构。

## 接口与不变量

- CI 的 Flutter 版本必须是**固定版本**，且下载包校验 SHA-256；不得使用 `latest`。
- CI 与本地使用同一组检查语义；`tooling/scripts/check.ps1` 与工作流不得出现两套规则。
- 检查失败必须让作业失败，不得使用 `continue-on-error` 掩盖。
- 密钥只存在于受保护环境，不进入工作流文件、日志或制品。
- 证据与制品只在必要时上传；不上传密钥、完整配对材料或用户文件。
- 新增 Action 必须记录用途、来源、维护状态与许可证（端侧设计 §12）。

## 验收条件

- [ ] `format / analyze / test` 在 CI 中可重复运行并通过。
- [ ] 协议固定向量、相对链接、敏感信息、依赖锁定四项在 CI 中运行并通过。
- [ ] Android 与 Windows 构建在 CI 中产出制品。
- [ ] Flutter 版本固定且校验 SHA-256。
- [ ] `.gitattributes` 生效且不产生无意义的内容改动。
- [ ] 本地 `check.ps1` 与 CI 检查范围一致。
- [ ] 失败路径被验证过（至少一次真实失败，证明门禁不是空转）。
- [ ] 台账与 README 如实更新。

## 必做测试

- 在本地运行完整 `tooling/scripts/check.ps1`（含新增两项）并保存日志。
- 对新增的两个 Python 检查脚本做**正反例验证**：注入一个坏链接与一个假令牌，确认脚本报错并非零退出。
- 用本地 YAML 解析校验 `ci.yml` 语法。
- 推送后检查真实 GitHub Actions 运行结果；失败必须修复而不是忽略。

## 交付物

- `.github/workflows/ci.yml`
- `.gitattributes`
- `tooling/checks/check_links.py`、`tooling/checks/check_secrets.py`、`tooling/checks/README.md`
- `tooling/scripts/check.ps1`（扩展）
- 证据：`docs/testing/evidence/2026-09-20/t01-02-01/`

## 人工复核点

- 工作流中是否出现任何密钥、令牌或可推断凭证的内容。
- 是否使用 `continue-on-error` 或空 `if` 条件让门禁形同虚设。
- `.gitattributes` 是否意外改变已提交文件的字节内容。
- 第三方 Action 的许可证与维护状态是否记录。

## 台账回填

- `docs/PROJECT_LEDGER.md`：§2（R02 或新增 CI 相关行）、§4（T01 说明）、§4.1（T01-02 状态）、§5、§7。
- `README.md`：补充 CI 状态与本地检查命令入口。
